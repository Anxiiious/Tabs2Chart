# SHRED2CHART — Sheet Happens → Clone Hero Converter

**Game plan / handoff doc.** This file is the source of truth for project state. It is read by
multiple coding agents (Claude Code and VS Code Copilot, swapped between token-limit breaks).
**Every agent session MUST update the "Current State" and "Decision Log" sections before ending.**
Do not assume conversational context — everything needed to resume lives in this file.

---

## 1. Project Goal

Convert Sheet Happens official Guitar Pro tabs (.gpx) into playable Clone Hero charts (.chart + song.ini), preserving the tab's tempo map so output is rhythm-synced to the real recording out of the box.

**Why this works:** Sheet Happens tabs are transcribed against the actual recordings, including tempo drift. Their tempo automations become the .chart SyncTrack nearly verbatim. Only a single global audio offset needs calibration per song.

### Non-goals (v1)
- General-purpose converter for arbitrary Ultimate Guitar exports
- Difficulty reduction (Hard/Medium/Easy) — Expert only
- Multi-instrument (bass/rhythm/drums) — lead guitar track only
- Automatic audio offset detection (manual via Moonscraper for now)
- Lyrics / vocal charts

### Target user
Single power user. Drop C metalcore / Drop A deathcore repertoire. Heavy open-string chug content — the open-note mapping matters a lot.

---

## 2. Architecture Overview

```
.gpx file
   │
   ▼
[Stage 1: Ingest]        .gpx → parseable form (see §3, two routes)
   │
   ▼
[Stage 2: IR]            Normalized tick-based event model (notes + tempo + TS + techniques)
   │
   ▼
[Stage 3: SyncTrack]     Tempo automations → B/TS events, ramps discretized
   │
   ▼
[Stage 4: Note Mapping]  6 strings × 24 frets → 5 lanes + open (the interesting part, §5)
   │
   ▼
[Stage 5: Emit]          .chart (text) + song.ini → CH song folder
```

Language: **Python**. Plain stdlib + PyGuitarPro where possible. No framework. CLI tool:
`shred2chart input.gpx --out ./songs/ArtistName - SongTitle/`

---

## 3. Stage 1 — Ingest (.gpx problem)

.gpx = Guitar Pro 6 container. **Not a zip** — proprietary BCFS (binary file system) wrapping an XML score (`score.gpif`). PyGuitarPro does NOT read it (caps at .gp5).

### Route A (prototype path — do this first)
Batch-convert .gpx → .gp5 externally (Guitar Pro or TuxGuitar), parse with PyGuitarPro.

**MANDATORY verification before committing to Route A:** convert one Sheet Happens song, dump all tempo events pre-conversion (from the .gpx XML) and post-conversion (from PyGuitarPro's parsed .gp5), diff them. If tempo automations are lost or quantized to bar boundaries, Route A is dead — go to Route B.

### Route B (direct parse — fallback / v1.1)
Reverse-engineered BCFS readers exist (alphaTab in JS/C#; Python implementations exist — research before writing one from scratch). Decompress container → extract `score.gpif` → parse XML. The GPIF XML is cleaner than the .gp5 binary: tempo automations are explicit `<Automation>` elements with bar position + value. No lossy conversion, no Guitar Pro dependency.

**Decision:** start Route A to validate the full pipeline end-to-end. Promote Route B only if A drops tempo data or the external conversion step becomes a workflow drag.

**Update (2026-07-16):** The `.gpx` → `score.gpif` extraction step of Route B got built anyway (`shred2chart/gpx_reader.py`), because it's needed regardless of which route wins — it's the only way to get "pre-conversion" tempo data to diff against for the M0 check itself. See Current State below for validation status.

---

## 4. Stage 2 & 3 — IR and SyncTrack

### Intermediate representation
Everything normalizes to a tick-based event list BEFORE mapping decisions. .chart standard resolution: **192 ticks per quarter note** — use it internally so Stage 5 is a straight dump.

Per-note IR fields:
- `tick` (position), `duration_ticks`
- `pitch` (MIDI number), `string` (1–6/7), `fret`
- technique flags: `hammer_on`, `pull_off`, `tap`, `slide_in/out`, `palm_mute`, `dead_note`, `bend`, `vibrato`, `tremolo_picked`
- `chord_id` (notes struck simultaneously share one)

### SyncTrack generation (first-class feature — the whole point of the project)
- Extract every tempo automation and time-signature change → `B <bpm*1000>` and `TS <num>` events at tick positions.
- **Linear tempo ramps:** GP supports gradual tempo automation; .chart has no ramp concept. Discretize: one stepped B event per beat across the ramp span. Per-beat resolution is sufficient.
- **Mid-bar tempo changes:** legal in GP, legal in .chart. Do not assume tempo events land on barlines.
- **Anacrusis / pickup measures & count-in bars:** classic off-by-one-measure bug. Handle explicitly in tick math — bar 1 of the GP file is not necessarily tick 0 of musical content.
- **Global audio offset:** single `Offset` value in .chart / `delay` in song.ini. v1: default 0, user calibrates in Moonscraper. Leave a `--offset-ms` CLI flag.

---

## 5. Stage 4 — Note Mapping (the actual hard part)

Core principle: this is **lossy compression, not translation**. Optimize for "feels like the riff," not literal encoding.

### Rules, in priority order
1. **Open-string chugs → CH open note** (`N 7`). Any note on the lowest string at fret 0 (or the primary chug pitch in drop tunings) maps to open. Palm-muted low-string runs are the bread and butter of the target repertoire.
2. **Pitch contour, not absolute pitch.** Sliding window (start: ~2 bars) tracks relative melodic motion. Riff ascends → lanes ascend. Reset/re-center window at phrase boundaries (rests ≥ 1 beat, or section markers from the GP file). Absolute mapping is forbidden — a 1-octave riff and a 3-octave solo both have to fit 5 lanes.
3. **Chords by interval spread:** power chords (root+5th) → two adjacent lanes. Wider voicings (root+octave+, 3+ note chords) → wider lane spread, max 3 lanes for playability. Chord root follows the contour rule; spread is relative to root.
4. **Repeated notes stay on the same lane.** Do not jitter identical consecutive pitches across lanes.
5. **Techniques → CH mechanics:**
   - hammer-on / pull-off flags → forced HOPO (`N 5` flag)
   - tap flag → tap note (`N 6` flag)
   - fast slides → trill-style lane walk; slow slides → sustain on origin note
   - tremolo picking → keep as individual notes (CH players expect the strum wall)
   - dead notes / ghost notes → same lane as neighboring context, no special marking (v1)
6. **Sustain threshold:** notes shorter than ~1/8 at local tempo get zero sustain (CH convention). Longer → sustain = duration_ticks, trimmed to leave a gap before the next note on that lane.

### Tuning knobs (expose as config, tune against real output)
- contour window size, phrase-boundary rest threshold, max chord width, sustain cutoff, HOPO auto-threshold distance.

### Quality bar
Output should be playable and *recognizable* without manual edits; Moonscraper is a polish pass, not a rescue pass.

---

## 6. Stage 5 — Emit

- **.chart format, not notes.mid.** Plain text, trivially writable, Moonscraper-native. Sections: `[Song]`, `[SyncTrack]`, `[Events]` (section names from GP markers — free flavor, do it), `[ExpertSingle]`.
- **song.ini** alongside: name, artist, charter, delay, diff_guitar, song_length.
- Output a complete CH song folder. Audio file placement is the user's job (drop song.ogg in the folder); tool prints a reminder.
- Reference spec: the community .chart format doc (Moonscraper repo / FireFox's .chart spec). Agent: fetch and pin the exact `N` flag semantics before writing the emitter — do not code note flags from memory.

---

## 7. Milestones

- [x] **M0 — Route A validation:** superseded by direct GPIF parsing, which turned out to be necessary anyway since real files are GP7 zip (§3). Validated tempo fidelity against 3 real files (constant tempo, real tempo changes, real time-signature changes) — GO on direct parsing. See Current State.
- [~] **M1 — Parse & IR:** IR event list, both via PyGuitarPro (`.gp3/.gp4/.gp5`) and direct GPIF parsing (`.gp`/`.gpx`) — see Current State. Dumped as JSON and spot-checked programmatically (note counts, chord grouping, every technique flag exercised) against 3 real files. **Not yet done:** the milestone's own verification step — visually spot-checking note count/positions against the tab open in Guitar Pro — needs a human with the GUI; no coding agent session has that. Consider this milestone open until someone does that check.
- [~] **M2 — SyncTrack:** built (`chart_writer.py`); emits B/TS events at 192 res from the IR tempo events, spec-pinned syntax. Awaiting the human Moonscraper/audio verification step.
- [~] **M3 — Emitter skeleton:** built (`mapper.py` + `convert` CLI); naive pitch-mod-5 mapping plus the three cheap high-value rules (tie merge, open-note chugs, HOPO/tap flags) and section-level track blending. Full charts generate for all real files. Awaiting the human playtest.
- [x] **M4 — Real mapping:** v1-v5 (static-anchor contour, proportional stepping, error-leak, HOPO/all-note collision nudges), v6 (rank-order-within-group + whole-song memoization, replacing the whole contour approach), v7 (chord grouping rolling anchor), v8 (chord group size capped to available lanes), v9 (single-note grouping by fret, not pitch), v10 (chord memo scoped per-group, not global) — ten real-bug iterations, each driven by an actual full-song playtest, not speculative tuning. See Current State for the full history. **Playtested end-to-end and confirmed good** by the user as of 2026-07-19, with one known, deliberately-deferred exception: chords currently always voice as physically-adjacent lanes (e.g. a power chord's root+5th sit next to each other), which is correct for *how a chord is fretted* but the user wants a follow-up pass that can spread genuinely different chord shapes across non-adjacent lanes too, for variety/playability rather than strict fretting realism — tracked as a fresh, separate item, not blocking M4's close-out. See §11 and Open Questions.
- [x] **M5a — Playback-order timing fix:** repeat barlines, 1st/2nd endings, and D.S. al Coda navigation are now simulated (`gpif_tempo.compute_playback_order`), so charts stay in sync with the audio through repeats and jumps instead of drifting. See Current State 2026-07-19.
- [x] **M5b — CLI polish:** `--offset-ms`, `--lead-in-bars` (default 2), `--track` (chart one track verbatim), `--audio`/`--album-art` (ffmpeg-backed conversion, `shred2chart/media.py`) all done. Offset/delay sign bug fixed same session (see Current State). Still open: batch mode, song.ini metadata pulled from the GP file header (currently only name/artist/album/genre/year if present — verify against a real file with all fields populated).
- [ ] **M6 — App container (new, user-requested 2026-07-19):** wrap `convert` in a small drag-and-drop app — drop a `.gp`/`.gpx` + an audio file in, get a finished song folder out, no terminal required. See §11 for the design questions and options; not started.
- [ ] **v1.1+ (parking lot):** Route B direct .gpx parsing · difficulty reduction · onset-detection auto-offset · rhythm/bass tracks · non-adjacent chord-lane spreading for playability (see M4 note above).

Each milestone's verification step is mandatory before checking it off. M2 especially — if the SyncTrack is wrong, everything downstream is wasted work.

---

## 8. Current State

> **AGENTS: update this section every session. Format: date, what was done, what's next, any blockers.**

- 2026-07-16 — Planning complete, this doc created. Nothing coded. Next: M0.
- 2026-07-16 — Repo scaffolded (Claude Code session). Built:
  - `shred2chart/gpx_reader.py` — decompresses BCFZ and unpacks the BCFS virtual filesystem to extract `score.gpif` straight out of a `.gpx`, no Guitar Pro/TuxGuitar needed for this step. Implemented from the reverse-engineering writeup at github.com/Antti/rust-gpx-reader (best public source found), with one correctness fix over that reference (its back-reference copy truncated overlapping runs; ours copies byte-by-byte so repeats work). Covered by unit tests using hand-built byte fixtures — **not yet validated against a real .gpx file**, since none exists in this repo or environment.
  - `shred2chart/tempo.py` — dumps tempo + time-signature events (tick, bpm / numerator+denominator) from a parsed `.gp3/.gp4/.gp5` via PyGuitarPro. Validated with a real PyGuitarPro round-trip test (write a song with tempo + TS changes, parse it back, confirm the dump matches).
  - `shred2chart/cli.py` — `shred2chart dump-gpif`, `dump-tempo`, `verify-m0` commands.
  - `tests/` — 8 passing tests (`pytest`), covering both modules above.
  - **Blocker:** M0 can't actually be checked off without a real Sheet Happens `.gpx` file and a Guitar Pro/TuxGuitar conversion of it to `.gp5` — this environment has no GUI apps and no sample tab. Next agent/session: get a real `.gpx` from the user, run `shred2chart verify-m0 song.gpx song.gp5`, and make the GO/NO-GO call on Route A per §3.
- 2026-07-17 — **Major finding, changes the plan:** got a real user file (`Brag.gp` by The Home Team). It is **NOT** a GP6 `.gpx`/BCFS container — it's a **GP7 `.gp` file, which is a plain zip archive** (`unzip`-able, confirmed via `zipfile.is_zipfile`). `Content/score.gpif` inside decodes as plain UTF-8 XML with no encryption (GP version `7.6.0`; only GP8 is known to encrypt this file). If the rest of the user's Sheet Happens library is also GP7, **the BCFS/BCFZ reverse-engineering in `gpx_reader.py` may never be needed** — real files skip straight to zip extraction, which is one line of stdlib `zipfile`, not a reverse-engineered binary format.
  - Confirmed the real `score.gpif` schema for tempo/time signature (previously an open question, §10): `<MasterTrack><Automations><Automation><Type>Tempo</Type><Bar>N</Bar><Position>P</Position><Value>"bpm ref"</Value></Automation></Automations></MasterTrack>`, and per-measure `<MasterBars><MasterBar><Time>N/D</Time>...</MasterBar></MasterBars>` (one MasterBar per musical bar; `<Time>` is omitted when unchanged from the previous bar — this repo's parser now inherits correctly, and a test in `test_gpif_tempo.py` catches regressions on that specific point since an early implementation got it wrong). `Brag.gp` itself has a constant 123 bpm, straight 4/4 throughout, 98 bars — no tempo drift to test the interesting case, but section markers (`<Section><Text>`) are present ("Intro" etc.) which the game plan's Stage 5 wants for `.chart` `[Events]`.
  - Built `shred2chart/gpif_tempo.py`: parses tempo + time-signature events directly out of `score.gpif` XML, no Guitar Pro/TuxGuitar conversion involved at all. Output shape matches `shred2chart/tempo.py` exactly so the two are diffable. Unit tested (including the inherited-`<Time>` bug above) and confirmed against the real `Brag.gp`/`Brag.gpif`.
  - Extended `shred2chart/gpx_reader.py` to read zip-based `.gp` containers transparently (previously it just rejected zips with a "go unzip it yourself" error). `extract_gpif()` now works on both formats. Also added a check for the GP8-encrypted-content case (non-UTF8 or non-XML `score.gpif`) so it fails with a clear message instead of silently returning garbage.
  - `shred2chart dump-tempo` now dispatches by extension: `.gp`/`.gpx` → direct GPIF parse (`gpif_tempo`), anything else → PyGuitarPro (`tempo`). Confirmed working end-to-end against `Brag.gp`.
  - `shred2chart verify-m0` upgraded from "print both sides, you eyeball it" to an actual automated diff (matches events by type+value within a small tick tolerance, reports GO/NO-GO). Still useful for validating Route A specifically (does external GP5 conversion preserve tempo fidelity?), but for GP7 files that question is largely moot now — `dump-tempo` on the `.gp` directly already gets the answer with zero external dependencies.
  - **Net effect on M0:** for GP7-format tabs, M0 is effectively resolved as **GO for direct parsing (Route B)** — no conversion step, no GUI app, no PyGuitarPro dependency needed for tempo. Whether this generalizes to the rest of the user's library (and to note/technique data, not just tempo — that's M1, still untouched) is the open question for next session. `Brag.gp`/`Brag.gpif` are the first real reference files but are **git-ignored** (`test_data/`) as the user's personal content, not committed.
- 2026-07-17 — Second real file tested: `Bite The Hook` by Incendiary, this time a native `.gp5` (not converted from anything — `shred2chart/tempo.py`'s PyGuitarPro path, no code changes needed). Ran clean: constant 171 bpm, 99 measures, 4 tracks (`Guitar 1`, `Guitar 2`, `Bass`, `Drums` — confirms multi-track files are normal, Stage 4's "lead guitar track only" scope will need to pick the right track index, not always assume track 0 is the guitar we want... it does look like it here, but worth double-checking per song). More useful than `Brag.gp` for time-signature coverage: this song has real mid-song meter changes (4/4 → 6/4 → 4/4 repeatedly, later brief 3/4 sections), all correctly detected. Still no example of a *tempo* change (both real files so far are constant-tempo) — the open question about tempo ramps/mid-bar tempo automation ticks remains unverified either way.
- 2026-07-17 — Third real file: `A Pale Light Lingers` by Boundaries, another GP7 `.gp` (zip) — this one exported with a slightly different file set inside (`Content/ScoreViews/`, `Content/Stylesheets/`, `meta.json` present; `Brag.gp` didn't have those) but same `VERSION 7.0` / same `score.gpif` schema, extracted and parsed fine with no code changes. **First file with actual tempo changes** — 8 distinct tempos (200→160→200→108→140→120→107→140 bpm) at bars 0/8/20/36/48/64/72/105. Cross-checked the raw `<Automation>` XML directly against `gpif_tempo.py`'s tick output: every single one matches exactly, which is real validation of the Bar-index → tick math (previously only exercised at Bar 0). Still true for every real file seen so far, though: every tempo automation has `Position=0` (bar-start) and `Linear=false` (no ramp) — so the two riskiest open questions (mid-bar `<Position>` fraction semantics, and how to discretize a `Linear=true` ramp) remain completely untested. Worth specifically asking for a song known to have a tempo *ramp* (gradual speed-up/slow-down) or a click/tempo change that lands mid-bar, if one exists in the library.
- 2026-07-17 — **Started M1** (per-note IR). Researched the real `score.gpif` note schema directly from `Brag.gpif` and `Boundaries.gpif` (both already on disk from M0 work): top-level `<Bars>`/`<Voices>`/`<Beats>`/`<Notes>`/`<Rhythms>` collections cross-referenced by id (see `shred2chart/ir_gpif.py`'s module docstring for the full shape). Built two parallel extractors matching §4's per-note IR fields (tick, duration_ticks, pitch, string, fret, chord_id, technique flags):
  - `shred2chart/ir_gp.py` — via PyGuitarPro (`.gp3/.gp4/.gp5`), using `Note.realValue`/`.string`/`.value` and `NoteEffect` fields (hammer/slides/palmMute/vibrato/tremoloPicking/bend/ghostNote, plus `TappedHarmonic` for tap). Round-trip tested — and that test caught two real PyGuitarPro gotchas worth remembering: (1) `gp.write()` silently collapses multiple beats in one voice into one unless `Beat.status`/`Note.type` are explicitly set to `normal` (they default to `empty`/`rest`), and (2) chord note order isn't preserved across write/parse (seems to get re-sorted by string).
  - `shred2chart/ir_gpif.py` — direct GPIF parsing, same output shape. Confirmed against real data: `HopoOrigin`/`HopoDestination` (hammer-on/pull-off), `Slide`+`Flags`, `PalmMuted`, `Muted` (dead/ghost note — distinct property from `PalmMuted`, resolving an earlier ambiguity), `Bended`, `Tapped`, `Vibrato` (a direct Note child, not a Property), `LetRing` (empty element), `Tie`, and beat-level `<Tremolo>` (denormalized onto each note in that beat since the IR is note-centric). Duration math (`NoteValue` + `AugmentationDot` + `PrimaryTuplet`) mirrors PyGuitarPro's own `Duration.time` formula.
  - Refactored `gpif_tempo.py` to expose `compute_bar_grid()` so both the tempo and IR gpif-parsers share one bar-tick-grid implementation instead of duplicating it.
  - Added `shred2chart list-tracks` — **needed because of a real finding**: track 0 is not reliably "the guitar." `Boundaries.gp` has a `Rhythm Guitar` track (0) with zero interesting techniques and a `Lead Guitar` track (1) where every single technique flag (hopo, slide, palm_mute, dead_note, bend, tap, vibrato, tremolo_picked, let_ring, tied) shows up with a plausible nonzero count. `Brag.gp` has 5 tracks, three of them identically named `Overdriven Guitar` — which one is "the" lead isn't derivable from the name alone there. `dump-ir` takes `--track` rather than assuming an index.
  - All 15 tests pass (`pytest`); ran `dump-ir` against all 3 real files with no crashes (500/876/1111 notes respectively, once pointed at the right track).
  - **Not done, left for next session:** the milestone's mandated verification step (open the tab in actual Guitar Pro, eyeball note count/positions against the IR dump) needs a human with the GUI.
- 2026-07-17 — **Cross-checked our GP-parsing logic against editor-on-fire (EOF)**, the open-source chart editor the CH community already uses to import Guitar Pro files (github.com/raynebc/editor-on-fire, specifically `src/gp_import.c`). It only reads `.gp3/.gp4/.gp5` (no GP7 support either, which is reassuring — confirms this is a genuinely hard format, not something we're missing an easy library for). Reading its actual binary-parsing logic (not just running it) turned up one real bug and resolved two open questions:
  - **Bug found and fixed:** PyGuitarPro's `NoteEffect.hammer` (and GPIF's `HopoOrigin`) is set on the *origin* note of a hammer-on/pull-off pair, not the destination note that actually plays without picking. `ir_gp.py` had been exposing that flag as-is on the wrong note (`"hopo": effect.hammer`). Fixed by shifting it forward one note, with direction from EOF's own algorithm (destination fret < origin fret ⇒ pull-off, else hammer-on). `ir_gpif.py` already read the correct note (GPIF has an explicit `HopoDestination` marker), but still needed the same fret-comparison logic to get *direction*. Both modules now expose `hammer_on`/`pull_off` instead of one flat `hopo` — matching §4's field names for the first time. Verified the fix actually changes real output sensibly: `Brag.gp`'s old flat `hopo` count of 72 (double-counting both notes of each pair) became `hammer_on: 36` (correctly counting one flag per pair, on the right note).
  - **Resolved:** GPIF's `<Slide><Flags>` bitmask. EOF's source documents the exact GP5+ bit assignments (1=shift slide, 2=legato slide, 4=slide out downward, 8=slide out upward, 16=slide in from below, 32=slide in from above) and GP7's GPIF appears to reuse them unchanged — our one real example, `Flags=2`, decodes as a legato slide-out, consistent with its context. `ir_gpif.py` now exposes `slide_in`/`slide_out` booleans matching `ir_gp.py`'s shape (raw `slide_flags` kept too).
  - Both fixes are documented inline in `ir_gp.py`/`ir_gpif.py`'s module docstrings with the EOF source citation. All 16 tests pass; re-ran `dump-ir` against all 3 real files — every song now produces a sensible, non-doubled hammer_on/pull_off/slide_in/slide_out split.
  - Still unresolved: the hammer_on/pull_off direction inference (in both modules) tracks "the previous note" per track, not per string — a simplification EOF itself makes too, but it means the very first note after a chord (or the note right after a string change) could get misattributed. Not expected to matter for the target repertoire (single-note lead lines) but worth remembering for Stage 4.
- 2026-07-17 — **More EOF cross-checks, per request, looking for further gaps** (not just re-confirming the hopo/slide fix):
  - **Found and fixed a real completeness gap:** `ir_gp.py` wasn't exposing `ghost_note` or `accent` at all, even though PyGuitarPro's `NoteEffect` has real, correctly-populated `ghostNote`/`accentuatedNote`/`heavyAccentuatedNote` fields (confirmed by reading PyGuitarPro's own `gp3.py`/`gp5.py` source — bits `0x04`/`0x40` of the note flag byte, cross-referenced against EOF reading the identical bits the identical way). Added both fields. `ir_gpif.py` gained `accent` too (a real, confirmed `<Accent>N</Accent>` direct child of `<Note>`, same shape as `<Vibrato>`) — but **not** a real `ghost_note`: neither real `.gpif` file contains anything named "Ghost", so it's exposed as an honest always-`False` stub with a comment, rather than guessing a property name. Verified against real files: `Brag.gp` track 1 (not track 0!) has 15 accented notes — another instance of "the interesting data isn't always on track 0."
  - **Noted for Stage 4, not fixed now (out of M1's scope):** EOF doesn't treat a tied note as a new note event — it extends the *previous* note's sustain instead of creating a separate attack. Our IR currently reports each tied note as its own entry (correct raw data — `tied: True` is exactly the flag Stage 4 needs to do this itself), but whoever builds the note-mapping stage needs to remember to merge ties into the prior note's `duration_ticks` rather than treating them as new picks. Documented in both `ir_gp.py`'s and `ir_gpif.py`'s docstrings so it isn't lost.
  - Also confirmed (no fix needed, just cross-referenced): GP7's tuplet math generalizes cleanly past what EOF's GP3/4/5 binary reader has to special-case. EOF hardcodes ratios for sextuplets/septuplets/13-tuplets because the old binary format only stores a note count, not a ratio; GPIF's `<PrimaryTuplet num den>` gives the ratio directly, so `ir_gpif.py`'s formula doesn't need EOF's lookup table.
- 2026-07-17 — Fourth real file: `Still Searching` by Senses Fail (Sheet Happens album test). GP7.6 zip, same schema, parsed with zero code changes. Constant 123 bpm 4/4 (still no mid-bar Position or Linear ramp in any real file — increasingly looks like Sheet Happens charts against a constant click, which would moot the ramp-discretization worry for this library). Five tracks; Guitar 2 is the technique-rich lead candidate (hammer-ons, pull-offs, 5 taps, vibrato), Guitar 1 the palm-mute-heavy rhythm. **Materially strengthens the slide bitmask conclusion:** file contains slide_flags values {1, 2, 4, 20, 32} — and 20 = 16+4 (in-from-below + out-downward) only makes sense as a bit combination, confirming the EOF-derived GP5 bitmask really is reused by GP7 GPIF. The "only one real example (Flags=2)" caveat in the Open Questions is now effectively closed.
- 2026-07-17 — **M2 + M3 built: `shred2chart convert` produces a complete CH song folder** (notes.chart + song.ini) from a `.gp` file. Pipeline: sections (`gpif_tempo.dump_sections`, from the real `<MasterBar><Section><Text>` markers) → per-section track blending (`blend.py`) → naive note mapping (`mapper.py`) → spec-pinned emitter (`chart_writer.py`). Details:
  - **Spec pinned before coding the emitter**, per this doc's own mandate: TheNathannator's GuitarGame_ChartFormats (Format-Overview + 5-Fret-Guitar pages). N 0-4 = GRYBO, 5 = strum/HOPO flip, 6 = tap (overrides HOPO), 7 = open; `B <bpm*1000>`; `TS <num> [log2 den]` with exponent omitted for /4; `[Song] Offset` in seconds; Resolution = 192 emitted (IR's 960 divides exactly by 5).
  - **Track blending (user-requested feature, pulled forward from M4):** real Sheet Happens songs split guitar across tracks, so `convert` blends them — per GP section, each candidate track gets scored (note count + 2× per technique flag, so a sparse flashy lead outbids a chug wall) and the section is taken whole from the winner; switches only at section boundaries so phrases stay intact. Tested on `Still Searching`: sections alternate Guitar 1 (rhythm) / Guitar 2 (lead) plausibly, and the 5 tap notes from Guitar 2 all survive into the chart. Files with no section markers (e.g. `Boundaries.gp`) fall back to 8-bar windows. `--tracks 1,0` overrides; default = all guitar-named tracks.
  - **Mapping is still M3-naive** (pitch mod 5; chords = adjacent lanes, max 3 wide) but includes the cheap rules that matter for the target genre: tied notes merge into the previous note's sustain (the EOF-confirmed behavior, now actually implemented), fret-0 on the lowest-*tuned* string (inferred from pitch−fret per string, so drop tunings work) → open note N7, hammer_on/pull_off → N5 forced flip, tap → N6, sub-eighth notes get no sustain, sustains trimmed to leave a 1/32 gap. Contour mapping remains M4 and replaces `mapper._assign_lanes`.
  - 25 tests pass (blend scoring/tie-breaks, tick conversion, open-note rule, tie merge, chord width cap, flags, sustain trim, full chart text). All 3 GP7 real files convert end-to-end.
  - **Next / blockers:** the human verification for M2+M3 is now one single step — drop each song's audio as `song.ogg` into the generated folder, load in Clone Hero or Moonscraper, confirm (a) barlines track the recording and (b) the chart is recognizably the song. `--offset-ms` exists for audio calibration. Known naive-mapping artifacts to expect: lane choices jump around (no contour logic yet), and repeated identical pitches do stay on one lane only by virtue of pitch-mod-5, not by rule.
- 2026-07-19 — **First real playtest surfaced a genuine bug, now fixed: charts drifted progressively out of sync with the audio.** User playtested `Still Searching` (Senses Fail) and the guitar came in noticeably late, worsening as the song went on — not a fixed offset (`--offset-ms` can't fix a *growing* drift). Root cause: `gpif_tempo.compute_bar_grid` walked `<MasterBars>` in **written (score) order**, but Guitar Pro *plays* bars in **performance order** — repeat barlines replay bars N times, 1st/2nd endings are conditional, and D.S. al Coda directions jump around. A section written once but played twice was charted at half its real duration, so every repeat compounded the desync. Confirmed directly against the real file's `score.gpif` (user provided `07 Still Searching GP.gp`), which uses the full set of these features:
  - `<MasterBar><Repeat start="true|false" end="true|false" count="N"/>` — repeat barlines, including a one-bar `count=2` repeat matching an `x1` marker visible in the printed tab.
  - `<AlternateEndings>1|2</AlternateEndings>` — 1st/2nd endings.
  - `<Directions><Target>Segno|Coda</Target></Directions>` / `<Directions><Jump>DaCoda|DaSegnoAlCoda</Jump></Directions>` — full D.S. al Coda navigation (one Segno, one Coda, one of each jump — confirmed the same shape in 5 of the album's 12 GP files).
  - **Fix, shipped in two commits** (both only touch the direct-GPIF path, `gpif_tempo.py`/`ir_gpif.py` — the PyGuitarPro path `tempo.py`/`ir_gp.py` is untouched and still has this bug if ever exercised on a repeat-heavy file):
    1. `3af06c3` — added `gpif_tempo.compute_playback_order()`: walks `<MasterBar>` file order but expands `<Repeat>` count and `<AlternateEndings>` into the real play sequence. Re-plumbed `compute_bar_grid` to return a third `bar_source_index` list (play-position → file-order bar), since one written bar can now map to several tick positions — every consumer (`ir_gpif.dump_ir`, `dump_sections`, `dump_tempo_events`, the cli.py 8-bar blend fallback) was updated to index by play position instead of file-order bar index.
    2. `4d9587d` — extended the same `compute_playback_order` with Segno/Coda/DaCoda/DaSegnoAlCoda handling. The subtle part, found by cross-checking a second real file (`Shark Attack`, same album): a Segno/Coda/jump marker can land on the **same bar** as a repeat-start/end or an alternate-ending, so the two systems had to be evaluated together in one per-bar walk rather than as separate passes. Also subtle: `DaCoda` must NOT fire the first time the walk reaches that bar — it only fires on the return trip after the D.S. jump has already happened once (mirrors how a 2nd ending is only taken on a later pass). Other GP direction words (D.C., Fine, plain ToCoda) are unconfirmed against any real file and raise `GpifFormatError` rather than being guessed at.
  - **Verification:** cross-checked every section marker's computed tick (converted to wall-clock seconds at the song's 123 bpm) against the timestamps Sheet Happens bakes into the section names themselves (e.g. `[F] (1:45)`). Before the fix, `[F]` computed to 1:08 (37s early); after both fixes landed, it computes to 1:45.4 — every one of the 13 sections now lands within about a second of its embedded timestamp, from `[A]` (0:00) through `[K]` (3:31). 63 tests total, all passing (48 pre-existing + 15 new covering repeats/endings/D.S./Coda, including the real bar shapes pulled from both real files).
  - **User then flagged a second, related issue while playtesting:** the chart's first note lands on tick 0, so there's no highway scroll before gameplay starts — nothing to judge Clone Hero's audio calibration against. Added `chart_writer.add_lead_in()` and a `--lead-in-bars` flag (default 2, commit `100ebdf`): shifts every tempo/section/note tick forward by N bars of the song's starting tempo/time-signature, and pushes the chart's `Offset`/`delay` back by the matching duration so the *audio* (untouched) still lands under the new, later downbeat. One real gotcha caught and fixed before committing: `tempo_events`/`sections` live at the IR tick scale (960/quarter) while `chart_notes` are already at chart scale (192/quarter) — the same musical shift has to be computed once (in bars → quarters) and then applied at each scale separately, or the sync track and the notes end up shifted by different amounts. Verified against `Still Searching`: 2 bars @ 123bpm 4/4 = 3902ms; `Offset = 3.902` in `notes.chart`, first note/tempo/TS event at tick 1536 (= 2 × 768 chart-ticks/bar), and 1536 ticks at 192 res / 123 bpm = 3.902s — offset and highway timing agree exactly.
  - **Not yet done / next steps:** (1) the actual in-game playtest of this fixed+lead-in chart hasn't been reported back yet — that's the real verification this whole fix was for; (2) the `ir_gp.py`/`tempo.py` PyGuitarPro path has the identical written-vs-performance-order bug and was explicitly left unfixed (out of scope, see commit messages) — only matters if a `.gp3/.gp4/.gp5` file with real repeats gets converted through that path; (3) nested/multiple Segno-Coda structures, D.C. (Da Capo), and `Fine` markers are unconfirmed against any real file and will currently raise `GpifFormatError` — fine for now (every file checked uses the same simple one-Segno-one-Coda shape) but revisit if a file with one of those turns up.
- 2026-07-19 — **User playtested a multi-track blend of `Still Searching` (two tracks combined via `--tracks`) and the lead parts "ended up really weird."** Two separate fixes landed for this:
  - **Fix 1 — `--track N` (singular):** the auto-blend (`blend.py`) picks a per-section "winning" track by note density/technique-flag score, which works when tracks genuinely alternate lead/rhythm roles but produces a jumbled, incoherent chart when two tracks weren't written to alternate that way. Added `--track N` to `cli.py`'s `convert` command (mutually exclusive with `--tracks` via `argparse`'s `add_mutually_exclusive_group`): bypasses `blend_tracks` entirely and charts one track's IR notes verbatim. Verified: converting with `--track 1` produces exactly `ir_gpif.dump_ir(xml, track_index=1)`'s note count (3198), confirming no blending logic runs.
  - **Fix 2 — M4 contour-based lane mapping (the bigger one):** user pasted a suggestion from Gemini diagnosing the *other* half of "weird" — `mapper._assign_lanes` mapped every single note by **absolute pitch mod 5**, with zero memory between notes, so a smooth melodic line could jump between non-adjacent lanes purely from where raw pitch happened to land mod 5. This was already this project's own known-open M4 milestone (see `mapper.py`'s module docstring, unchanged since M3: "the real contour-based mapping is M4"). Gemini's suggestion (anchor/contour + chord finger-span) matched the project's own diagnosis; refined per explicit user direction into a **threshold-based static anchor**: the anchor (`anchor_pitch`/`anchor_lane` in new `_LaneContour` class) models a guitarist's hand position and stays fixed while the melody moves within a normal hand span (`HAND_POSITION_SEMITONES = 4`, matching a real 4-5 fret box), only re-centering on a genuine position-shift-sized leap (`abs(delta) > 4`). This is a meaningfully different design from "update the anchor every note," which the user explicitly rejected — the static-anchor version guarantees **pattern stability**: an identical riff played twice in the same hand position produces the identical lane sequence both times.
  - **A real implementation bug was caught and fixed during this session, not before commit:** the first version updated `anchor_lane` incrementally from the *previous note's* lane rather than keeping it a pure function of the static anchor — this broke pattern stability itself (a new unit test, `test_pattern_stability_same_riff_maps_identically`, caught it immediately: replaying the same 3-note riff produced `[0,1,2]` then `[2,3,4]` instead of matching). Fixed by making every note's lane a fresh computation from `(anchor_lane, sign(delta))`, never from the last note's lane. This also meant one authored test's original expectation (`[0,1,2]` for an ascending run all within one hand-position box) was itself wrong given the approved design — notes within the box that share the same *direction* from the anchor land on the same lane by design (direction matters, not exact distance, within one hand position); fixed the test rather than the code once the design intent was re-confirmed against the plan.
  - **Chord handling:** kept the existing width-adaptive modulus (`base = pitches[0] % (5 - (width-1))`) rather than a hardcoded `% 3` that appeared in Gemini's pasted sample (that would have been a regression — it doesn't adapt to stack width and can overflow past lane 4 for a 3-note chord). Added a "disjoint" case on top: chord pitches sorted, and any consecutive pair more than an octave apart (`DISJOINT_CHORD_SEMITONES = 12`, strictly greater than) gets a one-lane gap instead of stacking adjacent — e.g. a two-hand tapped octave shape. Exactly 12 semitones (one octave) is defined as NOT disjoint; this exact boundary is unit-tested since it's an easy off-by-one to get backwards.
  - **Verified against the real file:** re-walked a stretch of `Still Searching` track 1's actual note-to-lane mapping. A repeated octave power-chord (pitches 54/66, gap=12, correctly non-disjoint) locks to the same `[2,3]` lane pair every single repeat. A melodic descent 42→40→38 (each step within the hand-position box) steps down one lane at a time (`[2]→[1]`, then 38 hits the open-note/chug-string special case and correctly returns `[7]` regardless of contour). A big +14-semitone leap (42→56) re-centers and lands a controlled one step away (`[3]`), rather than the old `56 % 5 = 1` that bore no relation to the actual jump. 70 tests pass total (59 pre-existing + 11 new: 8 single-note contour scenarios, 3 chord-disjoint scenarios).
  - **Not yet done:** the actual in-game/Clone-Hero playtest of this contour-mapped, single-track (`--track 1`) `Still Searching` chart hasn't been reported back — that's the real verification for M4, same as the timing fix above. The `HAND_POSITION_SEMITONES = 4` and `DISJOINT_CHORD_SEMITONES = 12` constants are both named/exposed as tunables in `mapper.py`, explicitly flagged (in the module docstring and this entry) as starting points to revisit once real playtest feedback comes in, not settled values.
- 2026-07-19 — **M4 v1 playtested and found broken; v2 shipped fixing it.** User's in-game screenshot showed the exact failure the entry above hadn't caught: wide-ranging lead runs jammed onto just 2 adjacent lanes (blue/orange), high on the neck — "it wants to stay super high on the frets, so all the different notes are all the same buttons."
  - **Root cause, confirmed by tracing v1's actual code against real data** (not just reading it): pulled the exact IR pitch sequence that reproduces this (track 1, ticks 92000-110880, a 40-note lead lick spanning 17 semitones, pedal-toning between pitch 61 and notes up to 78). Nearly every transition in that real sequence is a "leap" by v1's `HAND_POSITION_SEMITONES=4` threshold, and v1's leap branch used a **flat `step = sign(delta)`** — so a 17-semitone swing and a 1-semitone swing moved the anchor by the exact same single lane step. Once the anchor drifted to lane 4 and clamped, the whole run could only oscillate between 3 and 4, which is exactly the screenshot.
  - **A second pasted "fix" (also from Gemini) was traced against the same real data and also failed**: its re-center snapped to `pitch % 2` (absolute pitch), which ignores leap magnitude/direction history entirely and produced *inconsistent* lanes for the repeated 61 pedal tone (0 or 4 depending on which anchor happened to be active) — worse than v1 in a different way. User's explicit call after seeing this traced out: reject any absolute-pitch snapping (would reintroduce the same class of bug pitch-mod-5 had, e.g. global pitch-bucketing would permanently glue drop-tuned chugs to green and solos to orange) and require **proportional step scaling from the current anchor lane** instead.
  - **Fix:** unified the in-box and leap lane computation into one formula:
    `lane_delta = round(delta / SEMITONES_PER_LANE)` (new constant, `SEMITONES_PER_LANE = 3`),
    clamped to `[0,4]`, with a `lane_delta == 0` fallback of `±1` so any nonzero move still
    goes somewhere. `HAND_POSITION_SEMITONES` is unchanged and still gates *whether the
    anchor re-centers*, but no longer also dictates a flat step size — those two concerns
    (does the hand move; how far does the lane move) were conflated in v1 and are now
    separate. Verified the exact real 40-note sequence now spans all lanes 0-4, and that the
    repeated pedal tone (61) settles onto one consistent lane after its first occurrence
    (its very first appearance is still relative to whatever anchor preceded it — an
    accepted, understood limitation, not a bug). 71 tests pass (70 from v1 + 1 new
    regression test hardcoding this exact real sequence, asserting >=3 distinct lanes are
    used — direct regression guard against this exact bug class recurring).
  - Reconverted `Still Searching` with `--track 1`; spot-checked both the specific
    previously-broken section (now uses all 5 lanes in its first 20 notes) and the whole
    song's lane distribution (all 5 lanes used, no lane starved: {0:83, 1:26, 2:48, 3:34,
    4:50} single-note events). **Not yet done:** the in-game playtest of v2 — same
    outstanding verification as v1, now against the corrected chart.
- 2026-07-19 — **M4 v3: fixed a third real lane-mapping bug (section [D] orange-button clustering), found via user playtest of v2.** User reported: "Section D has most all notes in the beginning of the riff as consecutive orange buttons, even though they are very different notes." Root cause traced against the real file (`Still Searching` track 1, start of section `[D]`, tick 76800+): the anchor happened to re-center right before this passage and land at an edge lane (`anchor_pitch=69, anchor_lane=4`) — a leftover instance of the same "landing at an edge leaves no headroom" bug v1/v2 already had, just triggered mid-song instead of at note 1.
  - **First two fix attempts, both real progress but incomplete:** (1) seed `anchor_lane` at `CENTER_LANE=2` instead of `pitch % 5` for the very first note — fixes the note-1 case but not a mid-song re-center landing at an edge; (2) always re-center to `CENTER_LANE` (not the just-computed `new_lane`) on every leap, not just the first note — fixes the mid-song re-center case, but a regression test written against the *exact* real stuck state (`anchor_pitch=69, anchor_lane=4`, no fresh leap) still failed: `[4,4,4,4,4,4,4,4]` for the first 8 notes. Reason: none of those 8 notes is actually a >4-semitone leap from 69, so the re-center-on-leap logic never fires at all — an anchor that's *already* stuck at an edge, with only small in-box moves following it, has no mechanism to ever recover.
  - **v3 fix (error-leak / "tank tread"):** on any lane computation that clamps against an edge (0 or 4), the clamp error shifts `contour.anchor_lane` the opposite direction to absorb the hit — `anchor_pitch` is deliberately left untouched (shifting both cancels out and buys no headroom; this exact mistake was caught before implementing, by running the arithmetic against real data instead of trusting a hand-traced suggestion). This is a distinct mechanism from the existing leap-based re-center to `CENTER_LANE`, which is still needed and was NOT removed: verified by running both real problem passages through the fix with and without the leap-reset branch. Section [D]'s stuck cluster is fixed by the error-leak alone (leap-reset or not, same result: `[4,4,3,4,4,3,3,4,0,3,3,2,3,3]`, first un-stuck at note 2 when 69→73 clamps at the lane-4 wall and leaks into `anchor_lane`). But section [E]'s real wide lead lick (the v1/v2 regression fixture) *regresses* without the leap-reset: the anchor never follows the genuine position shift up into the 76/78 cluster, flattening it from 5 distinct lanes down to 4. Keeping both mechanisms together gives the best result on both real datasets — they solve different problems (error-leak = absorb a wall hit within one hand position; leap-reset = follow a genuine hand-position shift).
  - Rewrote `test_real_section_d_regression_first_notes_not_stuck_at_edge` to assert the exact verified real-data output (`[4, 4, 3, 4, 4, 3, 3, 4, 0, 3, 3, 2, 3, 3]`) rather than just "more than one distinct lane," so a future regression pins the precise behavior, not just "not all identical." All 72 tests pass (71 from v2 + this rewritten test), including `test_real_lead_lick_regression_spreads_across_lanes` (section E) unchanged and still green.
  - **Not yet done:** the in-game playtest of v3 — same outstanding verification as v1/v2, now against this further-corrected chart. Constants (`SEMITONES_PER_LANE=3`, `HAND_POSITION_SEMITONES=4`, `CENTER_LANE=2`) are unchanged from v2; this fix adds a new code path (clamp-error leak into `anchor_lane`) rather than retuning any constant.
  - v3 was reconverted against the real file (`--track 1`) and directly traced against the actual `_assign_lanes` (not just the test fixture): section [D]'s first 20 note-groups now span lanes `{0,2,3,4}` instead of clustering on 4.
- 2026-07-19 — **M4 v4: fixed a fourth real bug — hammer-on/pull-off notes landing on the SAME lane as the note before them.** User playtest of v3 found this while confirming the section [D] fix: "there should be no hammer ons and pull offs in the same lane consecutively if the prior note was the same note. That makes no sense. You HOPO to a different fret, always." Distinct bug class from v1-v3 (those were all about the anchor getting *stuck*; this is about the proportional contour's normal, otherwise-tolerable rounding ambiguity becoming nonsensical specifically for HOPOs). Root cause: `SEMITONES_PER_LANE=3` bucketing can round two genuinely different pitch deltas onto the same lane (e.g. anchored at 69, both pitch 71 (`delta=2`, `round(2/3)=1`) and pitch 73 (`delta=4`, `round(4/3)=1`) land on lane 3) — fine for two unrelated notes, but a hammer-on/pull-off reusing the same lane as its predecessor implies fretting the same note twice, which isn't how a HOPO works. Confirmed 4 real occurrences in `Still Searching` track 1 (e.g. 71→73 at tick 84000, both hammer_on-flagged, both landing on lane 3).
  - **Fix:** a post-pass in `map_notes` (not `_assign_lanes` itself, which has no visibility into "the previously emitted chart note" once chords/error-leak are involved) walks the finished `chart_notes` list; wherever a `forced` (hammer_on/pull_off) single-lane note matches its immediate predecessor's single lane, nudge it ±1 toward the direction its pitch actually moved (clamped to `[0,4]`, falling back to the opposite direction if the nudge would go out of range). Required adding `pitch` to `ChartNote.source` for single-note groups (previously only `ir_tick` was carried through), since the nudge needs the raw pitch delta, not just the lane.
  - Verified against the real file: all 4 known collisions (ticks 84000, 121440, 121920, 168480) are resolved to zero same-lane consecutive HOPOs across the whole track. New regression test `test_hammer_on_never_repeats_the_previous_lane` reproduces the exact 69→71→73 real shape via `map_notes` (not the lower-level `_assign_lanes`, since this fix lives in the post-pass). 73 tests pass (72 from v3 + 1 new).
  - Reconverted `Still Searching` with `--track 1` after this fix landed.
  - **Not yet done:** the in-game playtest of v4 — same outstanding verification as v1-v3, now against this further-corrected chart.
- 2026-07-19 — **M4 v5: generalized the v4 HOPO-collision nudge to ALL consecutive single notes, not just hammer-ons/pull-offs.** User playtest of v4 found a further real instance of the same underlying bug on *picked* notes: "There is also a section in G where it's a fast descending-ish powerchord chug, but it all hits the green lane consecutively." Traced against the real file (`Still Searching` track 1, section `[G]`, tick 251520+): a descending single-note-string run, frets 7/5/4/5/4/2 (pitches 52/50/49/50/49/47), none hammer_on/pull_off flagged — anchored at 52, delta -2 (pitch 50) and delta -3 (pitch 49) both `round()` to `lane_delta=-1` at `SEMITONES_PER_LANE=3`, landing both on lane 1 with the resulting lane still in `[0,4]` (no clamp error, so the v3 error-leak never fires either). Four genuinely different frets collapsed onto lane 1 (green) for the whole run.
  - **Fix:** the v4 post-pass's collision condition (`note.forced and lanes collide`) was widened to fire on ANY consecutive single-note pair with a nonzero pitch delta whose lanes collide, dropping the `forced`-only restriction — same nudge-toward-pitch-direction logic, just broader triggering condition. This is a strict generalization: v4's own real HOPO collisions still hit the same code path and are unaffected.
  - Verified against the real file: the section [G] chug run's lanes are now `[0,1,0,1,0,1]` (52→0, 50→1, 49→0, 50→1, 49→0, 47→1) — alternating, every distinct fret visually distinct. A whole-file scan confirms zero remaining same-lane consecutive distinct-pitch pairs anywhere in the track (previously 4 HOPO collisions, now checked more broadly and all clear).
  - New regression test `test_picked_descending_run_never_repeats_the_previous_lane` reproduces the exact real fret sequence (52/50/49/50/49) via `map_notes` and asserts no two consecutive notes share a lane. 74 tests pass (73 from v4 + this new test); the existing `test_hammer_on_never_repeats_the_previous_lane` still passes unchanged, confirming the widened condition is a strict superset.
  - Reconverted `Still Searching` with `--track 1` after this fix landed.
  - **Not yet done:** the in-game playtest of v5 — same outstanding verification as v1-v4, now against this further-corrected chart.
- 2026-07-19 — **M4 v6: replaced the whole static-anchor contour with a rank-order-within-group + memoized-repeat design, after the section [G] fix (v5) still wasn't musically right.** User playtest of v5: "G section decending riff is better, but it should be anchored differently since there are more than two notes and it only plays as green and red HOPOs. It should be blue, yellow, red, yellow, red, green to follow the exact note changes." Root cause: `SEMITONES_PER_LANE=3`'s per-note proportional formula is too coarse a granularity for a run this size (5 semitones, 52→47) - deltas -2 and -3 both round to the same lane step regardless of the v5 nudge, so a run with 4 distinct pitches only ever gets pushed apart pairwise, never laid out as a whole. Retuning `SEMITONES_PER_LANE` down couldn't hit the user's target either (tried 1.0-2.5, traced each against the real sequence - none matched, and the user clarified the exact target lanes weren't load-bearing, just "spread out to reflect the real notes").
  - **User's own suggestion, tried and found actively harmful:** re-anchor fresh at every section boundary, so each section's repeats stay internally stable. Checked directly against the real file first: **every real repeated riff in this song crosses section boundaries** (a 40-note riff repeats between `[C]`/`[D]` and the `[A']` reprise; a wide lead lick repeats between `[E]` and `[A']` eleven times) - per-section reanchoring would make the `[A']` reprise get an independently-computed anchor with no relationship to how it was charted the first time, actively breaking the "same riff looks the same" property rather than helping it. Not implemented, logged here so a future session doesn't re-propose it without re-checking this.
  - **v6 design (see mapper.py's module docstring for the full writeup):** replaced `_LaneContour`/`_assign_lanes` entirely.
    1. **Hand-position grouping** (`_group_into_hand_positions`): consecutive single notes within `HAND_POSITION_SEMITONES` of the group's first pitch form one group - same threshold concept as v1-v5's anchor, but now used purely to decide grouping, not per-note stepping.
    2. **Rank-order-within-group** (`_rank_order_lanes`): a group's DISTINCT pitches are ranked and spread across consecutive lanes centered on a target lane - this is what actually fixes section [G], since it looks at the whole group's pitch set at once instead of one proportional step at a time. Verified directly: `[52,50,49,50,49,47]` -> lanes `[2,1,0,1,0,1]` (later nudged, see below) - every distinct fret gets its own lane.
    3. **Proportional leap between groups** (reuses `SEMITONES_PER_LANE`, always re-centers to `CENTER_LANE`) - same mechanism as v2/v3, now operating between groups instead of between individual notes.
    4. **Boundary nudge** (generalizes v4/v5): if a new group's first lane collides with the previous group's last lane despite a different pitch, shift the WHOLE new group by one lane (preserves its internal rank spacing), computed once and then baked into...
    5. **Exact-repeat memoization** (new in v6, prompted directly by the user's stability concern): the first time an exact pitch sequence is seen as one hand-position group, its final lane sequence is cached; every later occurrence of that exact sequence replays the cached lanes verbatim, skipping fresh computation - this is what actually delivers cross-section stability, given (2) above ruled out per-section reanchoring. **User's explicit tradeoff call:** memoized replay is unconditional even when it would reintroduce a same-lane collision at that particular boundary (a rare case - 2 found in the real file) - "prioritize stability for those 2," i.e. "the same riff looks the same every time" beats avoiding a rarer, smaller collision.
  - **Chords got the identical treatment** (`_chord_rank_lanes`/`_chord_lanes_sequence`), prompted by a separate user observation: "There can be gaps with chords as well, not all chords need to have directly adjacent lanes" plus "move one of the lanes from one chord to another similar chord, so playing feels more natural." Found the same absolute-pitch bug class chords always had (`base = root % (5-(width-1))`, zero memory of the previous chord): confirmed a real 3-semitone (minor 3rd) root shift, `(59,71)->(56,68)`, jumping the FULL lane range (`[3,4]->[0,1]`) for no good reason. Chords now use the same anchor/leap/memoize pipeline as single notes, keyed on chord root pitch; within a chord, its own distinct pitches lay out around the target lane with the existing disjoint-gap rule (>1 octave = 2-lane gap) preserved unchanged. Verified: the real `(59,71)->(56,68)` transition now moves by 2 lanes instead of jumping the full range; the width-3 cap and disjoint-gap tests all still pass.
  - **Verification against the real file (all of section D/E/G plus whole-track scans):** section [D] still spreads across multiple lanes (unaffected by the rewrite); section [E] still uses all 5 lanes; section [G]'s descending run is now `[2,1,0,1,0,1]` (was stuck on green/red before v6). Whole-track single-note collision scan: 2 residual same-lane consecutive-distinct-pitch cases remain, both confirmed to be the accepted memoization tradeoff (a memoized group replaying its cached lane collides with whatever happens to precede it that particular time, 8 total occurrences, only 2 of which collide) - not a new bug.
  - Test suite fully rewritten for the new API: `TestLaneContour` (v1-v5, keyed to `_assign_lanes`/`_LaneContour`) replaced by `TestHandPositionGrouping` and `TestSingleNoteLanes` (keyed to `_group_into_hand_positions`/`_single_note_lanes`), including a new `test_exact_repeat_group_replays_the_first_occurrences_lanes` test proving the core stability guarantee with an unrelated passage in between the two occurrences (not just back-to-back). `TestChordDisjoint` updated for `_chord_lanes_sequence`, plus two new tests (`test_repeated_chord_shape_reuses_its_lanes`, `test_small_root_shift_does_not_jump_the_whole_lane_range`). 76 tests pass total.
  - Reconverted `Still Searching` with `--track 1` after this fix landed.
  - **Not yet done:** the in-game playtest of v6 — same outstanding verification as v1-v5, now against this substantially-redesigned chart. This is the biggest single change to M4 so far (a full replacement of the mapping algorithm, not an incremental fix), so the playtest matters more than ever here.
- 2026-07-19 — **M4 v7: v6's chord fix was itself broken - fixed the chord grouping AND the rank-order spread, both in the same session.** User playtest of v6: "Chords feel significantly worse. Section F feels anchored at blue and orange during most of the chord changes. It's not good at all." Traced against the real file: section [F] cycles through 5 power-chord shapes (roots 56/57/59/61/62, all root+octave voicings) in tight rotation, and v6's per-chord leap+memo design (structurally identical to the single-note v1-v5 contour, just for chords) had the exact same bug class - several distinct shapes happened to memoize onto lanes 3-4 on first occurrence, then every later recurrence replayed that stuck-high memo regardless of the surrounding progression.
  - **User's own instinct, checked against real data first (same pattern as the earlier per-section-reanchor question):** re-anchor fresh at section boundaries wasn't proposed this time; instead the user pointed at the right structural fix directly - "remember the chords can be split in order to get plenty of playable notes in a 5 lane range," i.e. treat a cluster of nearby, genuinely different chord shapes the same way the single-note section [D] fix treats a cluster of nearby, genuinely different pitches: rank-order the group's distinct roots and spread them across the available lanes, instead of one leap-and-memo per chord.
  - **First fix attempt (rank-order chord roots within a chord hand-position group) was correct in principle but didn't fully work**, because chord grouping used the SAME fixed-first-root anchor as single notes (`_group_into_hand_positions`), and section [F]'s progression is a gradual walk (56-57-59-61-62, every step <=4 semitones, but the group's first root vs. its last exceeds `HAND_POSITION_SEMITONES`) - so the fixed anchor split the 5 roots into two separate groups partway through, and the rank-order fix only ever saw 3-4 roots at a time instead of the whole progression, still crowding real distinct chords together.
  - **Real fix: chord grouping uses a ROLLING anchor** (compare each chord's root to the PREVIOUS chord's root, not the group's first) - `_group_chords_into_hand_positions` no longer shares logic with the single-note `_group_into_hand_positions`, it's now its own function with this rolling comparison. This is chord-specific for now (single notes haven't shown a real bug from the fixed-anchor design, so it wasn't changed there) - flagged as worth revisiting if a similar gradual-walk bug ever turns up in single notes too.
  - **Verified against the real file:** section [F]'s 5 roots now form ONE group and spread across lanes `{0,1,2,3}` (was `{0,1}`/`{3,4}` split across two groups before); the section's lane usage went from 104-of-128 notes on `(3,4)` to a spread across `(1,2)`/`(2,3)`/`(3,4)`. Whole-track chord lane usage is now spread across 0-4 (was concentrated at the top before this fix). Zero pattern-stability violations (every exact-repeat chord shape still maps identically). D/E/G (all single-note fixes) confirmed unaffected, since this fix only touches the chord code path.
  - New regression tests: `test_real_section_f_regression_nearby_chords_spread_across_lanes` (asserts >=4 distinct base lanes for the real 5-root progression), `test_gradual_chord_walk_stays_one_group_despite_wide_total_span` (asserts the 5-root progression forms exactly one group, pinning the rolling-anchor mechanism directly), `test_repeated_chord_in_a_fast_progression_reuses_its_own_lane` (repeat-stability still holds within a fast-changing group, not just across separate groups). Replaced `test_small_root_shift_does_not_jump_the_whole_lane_range` (its exact assertion no longer held once 2-chord groups correctly spread across the FULL available range rather than a constrained leap - confirmed this is correct new behavior, not a regression, before replacing the test). 78 tests pass total.
  - Reconverted `Still Searching` with `--track 1` after this fix landed.
  - **Not yet done:** the in-game playtest of v7 — now the third round specifically on chord voicing (v6 broke chords, v7 attempts to fix v6). Given the chord path has now needed two real iterations in immediate succession, this playtest matters more than most.
- 2026-07-19 — **Full-song playtest of v7 done; three more real bugs found and fixed (v8, v9, v10), plus the audio offset sign was backwards the whole time.** User played through the entire `Still Searching` chart (not just spot-checked sections) and gave section-by-section feedback across several passes. All four fixes below are committed together (`89877d6`); 79 tests pass.
  - **Offset/delay sign was inverted.** User reported having to manually override Moonscraper's Advanced offset field to `-3.4` every single export, no matter what the tool wrote. Root cause: `chart_writer.build_chart`/`build_song_ini` wrote the lead-in shift's raw value as `Offset`/`delay` (positive), but since the 2-bar lead-in shifts the CHART later while `song.ogg` is untouched, the audio needs to start LATER relative to the chart's new tick 0 - which this format's sign convention (confirmed empirically, not documented anywhere findable) expresses as NEGATIVE. Fixed by negating `offset_ms` at the two emission sites. Verified: now writes `Offset = -3.902` / `delay = -3902` for the real file, matching the sign and rough magnitude of what the user had been correcting to by hand. One test's expected value flipped to match (`Offset = -0.25` for a 250ms input).
  - **v8 — chord groups could hold more distinct roots than lanes could fit.** User: "H section... still stuck at 3 shapes despite [7 real distinct roots]." Traced to `_group_chords_into_hand_positions`'s rolling-anchor grouping (from v7) having no cap on group size - a slow-creeping progression (roots walk 54-57-59-61 then back down through 53-56-52-51, each step small) stayed one group even once it held more distinct roots than the 5-lane highway (minus a chord's own internal-voicing width) could give each one a unique lane. `_rank_order_chord_roots`'s plain proportional rounding then silently collided two different roots onto the same lane - a real chord change disappearing. Fixed by capping group size dynamically: a group is cut off once accepting the next distinct root would exceed `5 - group's-max-chord-width` lanes, so `_rank_order_chord_roots`'s rounding is provably collision-free by construction (not just usually fine). One test's exact assertion (`len(groups) == 1` for the 5-root section-F progression, all root+octave/width-1 chords) had to flip to `== 2`, since 5 roots genuinely cannot each get a distinct lane when only 4 slots exist after width is reserved - confirmed this is correct new behavior (0 collisions, full 5-lane spread across the 2 groups) before changing the test.
  - **v9 — single-note hand-position grouping used PITCH distance, not FRET distance.** User: "Section E lead part... red orange red orange... more than those two notes." Traced to the real lick (track 1, ticks ~92000-110880): a low pedal tone (pitch 61, string 3, **fret 11**) alternates against a moving voice on strings 4-6 at **frets 9-15** - pitch deltas up to 17 semitones (always exceeding `HAND_POSITION_SEMITONES=4`), but the whole phrase sits in a tight 6-fret span. Grouping by pitch fragmented it into ~20 isolated one/two-note groups, each leaping independently from a reset center lane, so several genuinely different notes collapsed onto the same clamped lane. This is the same class of bug v8 hit for chords, but for a different underlying reason (string-crossing, not group-size-vs-lane-count). **Considered and rejected an "octave-ish jump" heuristic first** - checked the real deltas (7, 8, 12, 15 semitones) and only one is a clean octave, so a modulo-12 detector wouldn't have caught this phrase at all. The real, correct signal was already sitting unused in the IR: `fret`. `_group_into_hand_positions`/`_single_note_lanes` now take `(pitch, fret)` pairs - grouping/hand-position-boundary decisions use fret (what actually costs the hand a position shift), lane rank-order still uses pitch (melodic contour). `map_notes`'s call site updated to pass real fret data through instead of discarding it before the call. Test signatures changed to match (`_pf()` helper added for synthetic pitch-only cases where fret==pitch is a fair stand-in); the real regression test now uses the actual real fret values and asserts no in-group lane collisions, not just "at least 3 distinct lanes" (which the old, weaker version passed even with this bug present - a false sense of coverage worth remembering).
  - **v10 — chord exact-repeat memo was global across the whole song, not scoped to the group that computed it.** User: "F... long section of blue orange chords that sound like there should be some movement." Traced to `_chord_lanes_sequence`'s `memo` dict living outside the per-group loop: chord shape `(59,71)` first appeared early in an unrelated hand-position group and memoized to `[3,4]`; a much later, completely different group (roots 56-57-59-61) computed 59's fresh rank-order lane as `[2,3]` - but the global memo intercepted the lookup first and replayed the stale `[3,4]` instead. Same thing independently happened to `(61,73)` and `(62,74)` in the same stretch, so three genuinely different chords all rendered identically, producing a 30-chord unbroken blue/orange run that the real underlying progression (roots cycling 59-61-62-61-59) did not warrant. Fixed by moving `memo = {}` inside the per-group loop (`group_memo`) - repeats within one group still replay verbatim (needed, unaffected: `test_repeated_chord_in_a_fast_progression_reuses_its_own_lane` still passes), but a different group's occurrence of the same shape now gets its own group's rank order instead of a stale one from elsewhere in the song.
  - **User pushed back correctly on a proposed "widen the disjoint-chord threshold" fix during v10 triage** - a pasted suggestion argued `>= 12` semitones (instead of `> 12`) would make ordinary root+octave power chords "split" for more visual variety. Checked against `test_exactly_one_octave_apart_is_not_disjoint` first: an exact octave is the single most common two-note voicing in this repertoire, and it's supposed to render as physically-adjacent lanes because that's how it's actually fretted - flipping the threshold would have gapped every ordinary power chord in the song, which is a regression, not the fix. Not implemented. The user's follow-up ask ("you don't have to map all power chords to adjacent lanes... split them to make different notes playable") is a real, separate, NOT-yet-implemented feature request - see §11 App Container plan is unrelated; this is logged as a fresh open item below and in Open Questions, since it needs its own design pass, not a threshold tweak.
  - Reconverted `Still Searching` with `--track 1` after all four fixes landed; user confirmed the chart "looks great now" aside from the adjacent-lane-chords item above.

---

## 9. Decision Log

> **AGENTS: append decisions with rationale. Never silently reverse a logged decision — log the reversal.**

- 2026-07-16 — Route A (external .gpx→.gp5 conversion) chosen as prototype path; Route B (direct BCFS parse) deferred pending M0 results.
- 2026-07-16 — .chart output chosen over notes.mid (text format, Moonscraper-native, trivial emitter).
- 2026-07-16 — Expert/lead-only scope for v1; difficulty reduction explicitly deferred.
- 2026-07-16 — Internal resolution fixed at 192 ticks/quarter to match .chart convention.
- 2026-07-16 — Built the `.gpx` → `score.gpif` extractor (normally Route B territory) ahead of schedule, because M0's own verification step needs a "pre-conversion" tempo source and there's no other tool available to produce one. This does not change the Route A vs B decision — it's shared infrastructure both routes need. Implementation follows github.com/Antti/rust-gpx-reader's reverse-engineered BCFS/BCFZ spec (no official spec exists); flagged as unvalidated against real files until M0 runs.
- 2026-07-17 — **Reversal candidate, not yet fully decided:** Route A (external .gpx→.gp5 conversion via Guitar Pro/TuxGuitar) was chosen 2026-07-16 as the prototype path. Real user data shows the actual files are GP7 zip-based `.gp`, where direct GPIF parsing (Route B) requires zero reverse engineering and zero external tools — it's strictly simpler than Route A for this format. Not logging this as a full reversal yet because (a) it's only confirmed for tempo data on one song, not notes/techniques (M1 territory), and (b) we don't yet know if 100% of the user's library is GP7 or if some older `.gpx` GP6 files are mixed in. Next session should get a second real file (ideally one with tempo changes, to actually test the Position-field assumption) and, if GP7 holds up, promote direct GPIF parsing to the primary path in §3 rather than a "fallback."
- 2026-07-19 — **Scoped D.S. al Coda / repeat-navigation fix deliberately to the direct-GPIF path only, not PyGuitarPro.** `ir_gp.py`/`tempo.py` (the `.gp3/.gp4/.gp5` path) have the identical written-order-vs-performance-order bug that `gpif_tempo.py` had, but every real file seen so far that actually needs this (has repeats/D.S.) is a `.gp` (GP7), so the direct-GPIF path is where it mattered. Revisit if a `.gp3/.gp4/.gp5` file with real repeat/D.S. structure surfaces — the same `compute_playback_order` design should port over, but it hasn't been written for that path.
- 2026-07-19 — **D.S. al Coda navigation scoped to exactly what's confirmed: one Segno, one Coda, one DaCoda jump, one DaSegnoAlCoda jump per file.** This is deliberate, not an oversight — 5 real files all show this exact shape and none show D.C. (Da Capo), `Fine`, multiple Codas, or nested Segnos. Per this doc's own "don't code format flags from memory" rule, those unconfirmed directions raise `GpifFormatError` instead of being guessed at. If a file with one of those turns up, extend `compute_playback_order`'s direction handling then — don't preemptively add speculative support.
- 2026-07-19 — **M4 contour anchor: static (threshold-based), not always-updating.** ~~User explicitly chose a design where the anchor only moves on a >4-semitone leap...~~ **Superseded 2026-07-19 by M4 v6**: the whole per-note static-anchor contour (`_LaneContour`/`_assign_lanes`) was replaced by hand-position-group rank-ordering plus whole-song exact-repeat memoization, after v5 still couldn't spread a real descending run out musically (a granularity limit of the per-note proportional formula itself, not a bug in the static-vs-always-update choice) and the user asked for cross-section riff-repeat stability that the old design's local-anchor-only memory couldn't provide. See Current State's v6 entry for the full design and why. This entry is kept for the historical record of v1-v5's reasoning, not as current guidance — the v6 entry supersedes it.
- 2026-07-19 — **M4 v3: error-leak absorbs edge clamps by shifting `anchor_lane` only, never `anchor_pitch`.** (Historical - superseded by v6 along with the rest of the contour design, see above.) A user-shared Gemini suggestion for this same fix proposed shifting both `anchor_lane` and `anchor_pitch` together on a clamp ("tank tread" framing) — traced against real data before implementing and found this cancels itself out: moving both by the equivalent amount preserves the exact same relative mapping, so no headroom is actually gained. General note reaffirmed (still applies post-v6): pasted AI suggestions are a second opinion on the *idea*, not literal code — verify the actual arithmetic against real data.
- 2026-07-19 — **Kept the existing chord base-lane modulus (`% (5 - (width-1))`) over a hardcoded `% 3` that appeared in a pasted Gemini code sample.** ~~The hardcoded version doesn't adapt to chord width...~~ **Superseded 2026-07-19 by M4 v6**: the absolute-pitch modulus itself (both the rejected `% 3` and the kept `% (5-(width-1))`) turned out to have the same bug class as the old single-note pitch-mod-5 mapping — confirmed on a real 3-semitone chord root shift jumping the full lane range. Chords now use the same anchor/leap/memoize pipeline as single notes (`_chord_rank_lanes`/`_chord_lanes_sequence`), keyed on chord root pitch. General note for future sessions, still valid: when the user shares AI-generated code suggestions, treat them as a second reference for the *idea*, not literal code to paste in — verify function signatures, existing logic, and edge-case math against the real files before adopting.
- 2026-07-19 — **Rejected per-section anchor reset for M4 v6, in favor of whole-song exact-repeat memoization.** User's own suggestion, given real consideration: reset the lane anchor fresh at every section boundary, so repeats within a section stay stable without needing memoization. Checked directly against the real file before implementing anything: every real repeated riff in "Still Searching" crosses section boundaries (the `[A']` section is a reprise of material from `[C]`/`[D]`/`[E]`) — per-section reanchoring would give the reprise an anchor with no relationship to the original, actively breaking repeat-stability rather than helping it, given how this song (and by extension, most songs with a reprised section) is actually structured. Logged so a future session doesn't re-propose this without re-checking against real repeat/section data first — the "reanchor per section" instinct is reasonable in the abstract, it just doesn't hold for real material with reprises.

---

## 10. Open Questions

- Does TuxGuitar's .gpx→.gp5 conversion preserve mid-bar and linear tempo automations? (M0 answers this.)
- Sheet Happens 7-string tabs (Drop A material): does the open-note rule apply to string 7 fret 0 only, or lowest-two-strings chug clusters? Decide during M4 with real material.
- GP "let ring" and harmonics flags — ignore or map? (Default: ignore, revisit if charts feel wrong.)
- ~~Does the `score.gpif` XML tempo `<Automation>` schema match what's assumed...~~ **Resolved 2026-07-17** — confirmed against a real file, now automated in `shred2chart/gpif_tempo.py`. See Current State.
- New (2026-07-17): is `<Position>` really a 0..1 fraction of the bar for mid-bar tempo changes? Only verified data point so far is `Position=0` (start of bar). Get a real file with an actual mid-bar tempo automation to confirm — if wrong, `gpif_tempo.dump_tempo_events`'s tick math for such events needs fixing.
- New (2026-07-17): is the whole Sheet Happens library GP7 (zip) format, or a mix of GP6 (`.gpx`) and GP7/8? If it's all GP7, the BCFZ/BCFS half of `gpx_reader.py` (§3 Route B's original scope) may be dead code worth deleting rather than maintaining unvalidated.
- ~~What do the `<Slide><Flags>` integer values in GPIF actually mean, bit for bit?~~ **Resolved 2026-07-17** — cross-checked against editor-on-fire's GP importer, which documents the GP5+ bitmask (1/2/4/8/16/32). See Current State. **Further confirmed 2026-07-17**: `Still Searching` contains flags {1, 2, 4, 20, 32}; 20 = 16+4 is a bit *combination*, which only works if these are genuine bitmask values. Considered closed.
- New (2026-07-17): which track is "the lead guitar" isn't always obvious — sometimes it's clearly named (`Lead Guitar` vs `Rhythm Guitar`), sometimes it's ambiguous (3 tracks all named `Overdriven Guitar` in `Brag.gp`). `dump-ir --track N` punts this decision to the user for now; Stage 4/M1 wrap-up should decide whether picking a track can be automated (e.g. "most technique flags" or "highest average pitch") or should just always ask.
- ~~The repeat/D.S. al Coda timing fix has been verified mathematically... not yet by an actual in-game playtest...~~ **Resolved 2026-07-19** — full in-game playtest done across many sessions; timing/sync itself was never flagged as wrong again after the fix landed. Considered closed.
- New (2026-07-19): does the `ir_gp.py`/`tempo.py` (PyGuitarPro, `.gp3/.gp4/.gp5`) path need the same repeat/D.S. playback-order fix as `gpif_tempo.py`/`ir_gpif.py`? Every real repeat-heavy file seen so far is a `.gp` (GP7, direct-GPIF path), so this hasn't been urgent, but if a native `.gp5` with real repeats ever gets converted, it will show the exact same progressive-drift bug this session just fixed on the other path.
- ~~`mapper.HAND_POSITION_SEMITONES` (4), `mapper.SEMITONES_PER_LANE` (3)... not tuned against real in-game playtest feedback yet...~~ **Resolved 2026-07-19** — all three constants (`HAND_POSITION_SEMITONES=4`, `SEMITONES_PER_LANE=3`, `DISJOINT_CHORD_SEMITONES=12`) survived a full end-to-end playtest unchanged; every bug found (v8-v10) was in the grouping/memoization *logic* around these constants, not the constants' values themselves. Not revisiting these numbers unless new real material suggests otherwise.
- New (2026-07-19): **non-adjacent chord-lane spreading for playability, not just musical/fret realism** — user's own framing: "spaced chords that don't have to make perfect musical or algorithmic sense, but playable sense," pointing at a reference chart (`Monkey Wrench` by Foo Fighters, a Rock Band `.sng` file) as an example of a professionally-charted song using non-adjacent lanes for what are, on the guitar, adjacent-fret power chords. Currently `_chord_internal_lanes` always keeps a chord's own 2-3 notes on physically-adjacent lanes (by design — "adjacent lanes = close together" is deliberate and tested, see `test_close_chord_stays_contiguous`/`test_exactly_one_octave_apart_is_not_disjoint`). The ask is different: deliberately break strict fretting-adjacency for SOME chords, purely to make a fast progression more varied/playable to strum, the way a human Harmonix/CH charter would. **Not designed yet** — needs its own pass, not a threshold tweak (a "widen the disjoint threshold" attempt was tried and correctly rejected during v10, see Current State). Attempted to pull the referenced `Monkey Wrench.sng` file apart to extract its actual `notes.chart` for a concrete before/after comparison, got the container's binary layout partially decoded (SNGPKG v1: file-level XOR-obfuscated custom format, ini-metadata block parses cleanly as length-prefixed key/value pairs, but the file table's exact field widths weren't fully pinned down before the user called it off) — a working `.sng` extractor would be a fast way to get a second real reference chart's exact chord-voicing choices, if this comes up again. Next session: design a heuristic (or hand-authored rule) for when to deliberately gap two chord notes for playability despite them being musically/physically adjacent, then verify against the real "Still Searching" file's fast progressions (section F is the known example).
- New (2026-07-19): **app container (M6)** — see §11 for the design questions this needs answered before implementation starts (target OS, packaging approach, ffmpeg bundling).

---

## 11. M6 — App Container (not started)

**Goal, in the user's own words:** "an app container so I can just drop a gp file and audio file
into a little 'program' and have it generate the chart, use ffmpeg to convert the audio, and spit
out a chart for me." I.e., no terminal, no `pip install`, no remembering flags — drag two files in,
get a `songs/Artist - Title/` folder out.

### What already exists to build this on top of

The CLI's `convert` command already does everything the app needs to call into:

- `.gp`/`.gpx` → notes.chart + song.ini (`cli._cmd_convert`, `mapper.py`, `chart_writer.py`)
- audio file → `song.ogg` via `shred2chart/media.py`'s `convert_audio()` (ffmpeg-backed, checks PATH
  first then a bundled `ffmpeg/bin/` folder — see `media.find_ffmpeg()`)
- optional album art → `album.png` via `media.place_album_art()`

So this milestone is **packaging and a thin UI shell**, not new conversion logic. The hard problems
(tempo sync, repeats/D.S., note mapping) are M0-M5's work and are already done.

### Open design questions (answer these before writing code)

1. **Target platform.** The user is on Windows (per this session's environment). Does "little
   program" mean:
   - a native Windows `.exe` (no Python install required on the target machine), or
   - a script the user runs from a folder they already have Python set up in (lower effort, but
     still requires the one-time `pip install -e ".[dev]"` from HANDOFF.md)?
   This changes the packaging tool choice a lot (see below) — ask the user directly rather than
   guessing, since "little program" could mean either.
2. **UI shape.** A few real options, roughly cheapest-to-build first:
   - **Drag-and-drop onto a `.bat`/shortcut**: user drags a `.gp` file (and optionally an audio
     file as a second selection, or drops both at once) onto an icon; a wrapper script infers
     `--audio` from a same-named file next to the `.gp`, runs `convert`, opens the output folder
     when done. Zero new dependencies, but "drop two files at once" onto a single icon isn't how
     Windows drag-and-drop works natively (usually one drop target = one dropped selection) — may
     need the user to drop the `.gp` first and have the script auto-look for a same-named audio
     file (`.flac`/`.mp3`/etc.) next to it, falling back to a second prompt if none is found.
   - **A minimal GUI window** (file pickers for "GP file" and "audio file" + a "Convert" button +
     a progress/log area) — more natural for "drop things into a little program," but is an actual
     new UI to build and test. Python's stdlib `tkinter` needs no extra dependency and can be
     packaged into a single `.exe` (see below); a web-based UI (e.g. a local Flask/FastAPI page)
     is more work for no real benefit here since there's no need for remote access.
   - **A folder-watcher**: drop files into a designated "inbox" folder, a background process picks
     them up and writes output next to them. More moving parts (a persistent process, pairing
     logic for "which audio file goes with which GP file") for not much UX gain over drag-and-drop
     — probably not worth it unless the drag-and-drop UX turns out to be too fiddly in practice.
3. **Packaging tool** (only matters if target is a standalone `.exe`, question 1): PyInstaller is
   the standard choice for turning a Python script + its dependencies (including PyGuitarPro) into
   a single Windows executable; no code changes needed to the existing package, just a build step
   and a spec file. Not yet evaluated against this specific project — first real risk to check is
   whether PyInstaller's dependency-bundling correctly picks up PyGuitarPro's data files (if any)
   and whether the resulting `.exe` size/startup time is acceptable.
4. **ffmpeg bundling.** `media.py` already looks for a bundled `ffmpeg/bin/ffmpeg.exe` next to the
   repo root before falling back to PATH (see `.gitignore`'s note on this — it's intentionally not
   committed, a large third-party binary). For a packaged app, decide: bundle ffmpeg inside the
   app's own folder (bigger download, works offline out of the box) vs. document "install ffmpeg
   once, add to PATH" as a one-time setup step (smaller app, matches how HANDOFF.md already talks
   about ffmpeg). Given the target user already has ffmpeg concepts in their workflow (audio
   conversion is already part of the ask), a documented one-time PATH setup is probably the lower-
   effort choice to start with — revisit if that turns out to be a real friction point.
5. **Error surfacing.** Right now `convert` prints warnings to stderr and keeps going (e.g. missing
   ffmpeg, missing audio file — see `cli._cmd_convert`'s `if args.audio:` block). A GUI needs those
   surfaced somewhere the user will actually see them (a log pane, a popup, a written log file next
   to the output) rather than a vanishing console window — this is probably the single most
   important UX difference between "CLI tool" and "little program," since a silent failure in a
   packaged `.exe` with no visible console is much worse than one in a terminal.

### Suggested first step, once the above is answered

Given the existing `convert` command already does the real work, the lowest-risk path is probably:
a minimal `tkinter` window (two file pickers + a "Convert" button + a scrolling text log fed by
redirecting `cli._cmd_convert`'s prints) as a new `shred2chart/gui.py` / `shred2chart-gui` entry
point, tested unpackaged first (`python -m shred2chart.gui`) before ever touching PyInstaller — get
the UI/UX right as a script first, packaging is a separate, later concern that shouldn't block on
answering questions 3/4 above until the UI itself is proven out.

**Not started as of 2026-07-19.** This section exists so a future session can pick a design option
above (ideally by asking the user directly, per this repo's own pattern of checking real
constraints before building) rather than guessing.

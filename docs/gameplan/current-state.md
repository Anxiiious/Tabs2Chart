# Current State

- 2026-07-26 — **GUI front end migrated from Tk to PySide6** (Claude, session `2026-07-26-claude-01`). The importer window was rebuilt in Qt because the Tk implementation's "glass" appearance was faked with `Canvas` primitives — `create_polygon(..., smooth=True)` for corners (bezier blobs, not radii) and per-2px gradient line loops (visible banding) — and Tk has no route to real border radii, gradients, hover transitions, or decent type. New files: `shred2chart/qt_theme.py` (palette + QSS; the check/chevron glyphs are written to a temp dir as SVG at runtime because QSS cannot inline them), `shred2chart/gui_common.py` (toolkit-free helpers, keeps `tests/test_gui.py` display-independent). `shred2chart/gui.py` is now the Qt window; the prior Tk version is preserved verbatim as `shred2chart/gui_tk_legacy.py` (unmaintained fallback). Layout was restructured from a single 900px vertical stack of six cards into: header bar → full-width import-summary card with accent rail and per-input status tiles → two columns (source material | chart controls in Mapping/Sync/MoonScraper tabs) → primary CTA → activity log → footer actions. Drag-and-drop now uses Qt's native file drops, so `tkinterdnd2` is no longer a dependency; conversion threading moved from `queue`-polling to a `QThread` worker with signals. `shred2chart.spec` drops all Tcl/Tk data/DLL wrangling and the `pyi_rth_tkinter` runtime hook, excludes unused Qt modules, and keeps UPX off the Qt DLLs. **Validation:** 128 tests passed (isolated workspace pytest base); window and per-measure dialog rendered offscreen and inspected in idle and populated states. **Still required:** install `PySide6-Essentials` in the project venv, launch on Windows to confirm Segoe UI metrics and DPI scaling, and rebuild `dist/Tabs2Chart.exe` (not rebuilt this session — the existing exe is still the Tk build).

- 2026-07-26 — **Dense-chord cap crash repaired** (Codex, session `2026-07-26-codex-11`). A GUI conversion of Still Searching with tracks `0,2` failed with a numeric `KeyError` when dense blended harmony yielded a dropped fourth pitch plus a duplicate of that dropped pitch. The mapper now remaps duplicates of capped representatives to the highest retained pitch before lane assignment. Reproduced the exact CLI equivalent successfully after the fix. **Validation:** 128 tests passed with an isolated workspace pytest base; rebuilt `dist/Tabs2Chart.exe` (SHA-256 `F1D2301A75F5C836E9982F3B409318FEE8F5D8FAEB4E3074F8DDDCD9C4E9D900`).

- 2026-07-26 — **Expressive dense-chord voicings generalized** (Codex, session `2026-07-26-codex-10`). Dense simultaneous twin-guitar passages now retain both parts as a three-lane playable chord, and any two- or three-note progression with two distinct chord changes within one bar earns a non-adjacent voicing where legal. Repeated chords retain their exact shape. The supplied Still Searching Section H now progresses through varied shapes such as `G/R/B`, `R/Y/O`, and `R/B/O` instead of adjacent three-button blocks. **Validation:** 127 tests passed with an isolated workspace pytest base; rebuilt `dist/Tabs2Chart.exe` (SHA-256 `E3C24DF4E81BBFC03E1FE8A7FCD871B08DB846944C14BBB9655EBB7C3B98CFF9`). Human playability validation remains required.

- 2026-07-26 — **Repeated-phrase family anchoring refined** (Codex, session `2026-07-26-codex-09`). Exact full-measure lane replay remains the rule for truly identical measures. For the common real-world case where two nearby bar-start phrases share at least four note-and-rhythm-identical events but their endings differ, the mapper now replays the shared lanes and plans the first occurrence from the closest variation's *own measure* continuation. This avoids both random same-chug lane changes and a distant, unrelated variation forcing an impossible compromise. The supplied Still Searching passage now has matching measure-15/16 chugs `R`, its harmony `R/Y`, and the lower measure-16 chugs `G` rather than `G → O`. **Validation:** 124 tests passed with an isolated workspace pytest base; regenerated the supplied GP chart; rebuilt `dist/Tabs2Chart.exe` (SHA-256 `70E98EEBB7ADB64F4C12CE3427BC4E3A93E2394AFF2CC077F4EB459EF762DA9E`). Human MoonScraper/playability validation remains required.

- 2026-07-26 — **Returning-riff memory corrected to exact-measure memory** (Codex, session `2026-07-26-codex-08`). Replaced the prior four-event fingerprint with a complete source-measure fingerprint: every fretted-note onset and its position inside the measure must match. When an identical measure occurs later, its already-emitted lane sequence is replayed exactly; a partial run or merely similar chord sequence does not qualify. This supersedes the immediately preceding session's riff-fingerprint wording. **Validation:** 123 tests passed; rebuilt `dist/Tabs2Chart.exe`.

- 2026-07-26 — **Returning-riff lane memory added** (Codex, session `2026-07-26-codex-07`). The mapper now fingerprints complete tab material before emitting lanes. When at least four note events with identical fretted notes and rhythm recur later—even after a section marker—it reuses the original event-by-event lane pattern. This is deliberately stricter than globally locking an isolated chord, so similar-but-not-identical material may still map independently. Same fretted chord voicings also remain stable within their section, including across rests. **Validation:** 123 tests passed; rebuilt `dist/Tabs2Chart.exe`.

- 2026-07-26 — **Bulk source-measure track selection added** (Codex, session `2026-07-26-codex-06`). The per-measure picker now has an inclusive bulk range row: select `From`, `through`, a named track or `Auto`, then **Apply range**. This supports an 80+ measure tab without losing the individual per-measure controls needed for exceptions. Focused GUI/pipeline tests: 54 passed; rebuilt `dist/Tabs2Chart.exe`.

- 2026-07-26 — **Windows GUI packaging repair** (Codex, session `2026-07-26-codex-05`). The first per-measure-override build crashed at startup with `ModuleNotFoundError: tkinter` because the Codex Python runtime omitted Tcl/Tk assets from PyInstaller discovery. Updated `shred2chart.spec` to package matching Tk source, `_tkinter`, Tcl/Tk DLLs and scripts, with a runtime environment hook. Rebuilt `dist/Tabs2Chart.exe` (SHA-256 `E9FC10D6E6767E9EA332CE27C16968AE9537C400F91A99503A724502E36EAE6F`) and verified it stays alive through a hidden three-second launch smoke test.

- 2026-07-26 — **Per-source-measure guitar track overrides added** (Codex, session `2026-07-26-codex-04`). Advanced now includes **Choose per measure...**, which reads the selected Guitar Pro file's original track names and shows every source measure as `Auto` or a forced track. Overrides are passed through the shared GUI/service/CLI path (`--measure-tracks 5:2,6:1`), apply to every playback occurrence of a source measure, and intentionally exclude generated lead-in measures. A forced silent track leaves that measure silent rather than silently falling back to another part. Rebuilt `dist/Tabs2Chart.exe`. **Validation:** 120 tests passed; manual GUI selection/playability verification remains.

- 2026-07-26 — **Still Searching harmony/wrap mapping revision delivered** (Codex, session `2026-07-26-codex-03`). Implemented and regenerated the actual `dist/songs/Senses Fail - Still Searching/notes.chart` from `07 Still Searching GP.gp`. Twin-guitar spans no longer alternate whole bars: the active higher register wins, and a simultaneous upper voice gets a tested wide-dyad preference. The visible chart now changes from `G/R` in measure 4 to the requested `R/B` in measure 5, rather than `R/Y` in measure 6. Phrase re-anchoring now examines only the first eight note groups; at a bar line it re-anchors only a descending-only opening. The later chug figure is now `R ... R -> G ... G` rather than `G ... G -> O ... O`. Regression coverage is in `tests/test_chart_pipeline.py`; targeted suite: 26 passed; complete suite: 116 passed using a workspace test base (the default Windows temp location is permission-blocked). The prior chart is retained as `notes.before-harmony-mapping.chart`; preview output is `output/still-searching-harmony-preview`. **Still required:** user visual/playability validation in MoonScraper/Clone Hero.

- 2026-07-26 — **Custom MoonScraper measure counter added** (Codex, session `2026-07-26-codex-01`). Added a fixed yellow `Measure N (tick T)` overlay at the upper-left of the Tabs2Chart custom MoonScraper build for every chart it opens; it coexists with the manifest-only alignment guide. It is live in edit and playback modes and derives its number from the same `TimeSignature.GetMeasureInfo().measureLine.tickGap` cadence used by upstream `DrawBeatLines`; time-signature boundaries count as new visible measures. Implemented in both the BepInEx runtime plugin and reproducible official-source patch, then recompiled the shipped plugin DLL (SHA-256 `D1A355CAAF813D39A396E5E95CCCDA08ABF93C967B779F69F3CECE455C2DE2AD`). **Validation:** the patch applies cleanly to pinned upstream `cb4c7a8`; compilation against the installed MoonScraper assemblies succeeds. **Still required:** a human needs to open a real generated chart, compare the counter to the beat-line grid through a meter change, and complete the pending early/late audio and Clone Hero checks.

- 2026-07-23 — **Alignment guide moved right and lead-in reduced to two bars** (Codex, session `2026-07-23-codex-06`). Moved both the open alignment window and collapsed F8 hint from x=16 to x=340: a 324-pixel shift, equal to 90% of the 360-pixel panel width. Reduced the shared conversion default from four tempo/meter-aware measures to two without changing the timing model; for the 120 BPM/4/4 fixture the first note is now tick 1536 with `Offset = -4.0` and `delay = -4000`. Updated the source plugin, reproducible upstream patch, tests, user docs, architecture, and M6 wording. **Validation:** 113/113 tests pass; rebuilt `dist/Tabs2Chart.exe` (49,854,601 bytes, SHA-256 `7CDE7EE21F0E730E957D3E66B128C3003C24B4F200EEE4B0C88BA3506A984675`); recompiled the BepInEx plugin (SHA-256 `DD5DBFC620A073BCA5ADF824CABC476D4F99301034FBC4F5AAE04465233BB53C`); generated-chart timing assertions pass; both packaged applications stayed running in smoke tests; the MoonScraper log confirmed plugin load and manifest activation. **Still required:** visually confirm the new panel placement and two-bar feel with a real song, then perform the pending Clone Hero sync check. The dirty/diverged local `main` remains unchanged.

> Local mirror of the [authoritative Notion page](https://app.notion.com/p/3a5b82db13b7810894b1daac0be1aa2e). Keep both copies synchronized when project state changes.

> 2026-07-20 — **Addressed 5 previously-flagged open gaps** (GitHub Copilot session): `Linear=true` tempo ramps now discretized into one stepped B event per beat with linear interpolation (falls back to a single instantaneous event if it's the last automation); two new synthetic-fixture tests. GP6 `.gpx` BCFS/BCFZ path: decided to keep, no code change (see Decision Log). `ghost_note` in `ir_gpif.py` upgraded from always-`False` stub to `"GhostNote" in props`, following GPIF's uniform boolean-property pattern; new synthetic test; flagged as still unverified against a real file. 7-string/Drop A open-note rule decided (see Decision Log). **59 tests pass.**
> Older session history is preserved in [Current State Archive](current-state-archive.md). Read it when the active summary points to earlier context.
## Summary as of 2026-07-22
Real Sheet Happens tabs are GP7 `.gp` zip files, parsed directly via GPIF (no Guitar Pro/TuxGuitar needed) — this is now the documented primary architecture (see Architecture §3 and Decision Log, 2026-07-22). Legacy `.gp3/.gp4/.gp5` go through PyGuitarPro; GP6 `.gpx` BCFS/BCFZ support is retained for compatibility but unexercised against a real file. M0-M3 are done. M4 (contour + chord-shape lane mapping) and M5 (CLI polish) are both substantially implemented — 63 tests passing, including a proven-not-just-tested cursor-resync-drift regression — but neither is checked off, because neither has been playtested by a human in Clone Hero/Moonscraper yet. The project's primary outstanding work is heuristic quality (does the mapper produce readable charts), not correctness (does the algorithm behave correctly) — see Open Questions' "next-iteration priorities" for the ordered list.
## Recent entries (full detail; older entries are in the archive)
- 2026-07-21 — **Resync-drift question actually settled, not just argued:** built a ~250-chord adversarial synthetic progression (ascending run, rest ≥1 bar, descending run with bigger steps, 30-chord plateau, section-marker reset, another ascending run with periodic octave leaps; 215/300 chords triggered a scoring override in the exploratory run). Compared each chord's actual emitted anchor lane against a baseline (the same sequence run as single notes, `k=1`, zero chord-scoring override involved). **Result: no drift, and it's structurally impossible, not just empirically absent** — (1) both actual and baseline lanes are always already-wrapped into 0-4 by `% 5`, so their difference is trivially bounded to `[-4,4]`, no unbounded quantity exists for error to accumulate in; (2) `_interval_to_step`'s step magnitude depends only on the real pitch interval, never on the cursor's absolute value, so a resync is a one-time phase-shift with no mechanism to bias later step size/direction. Empirically confirmed: front-half vs back-half mean deviation from baseline were statistically indistinguishable, no growth. Turned into a permanent regression test, `test_cursor_resync_does_not_drift_over_long_progression`. **63 tests pass, 0 xfail.**
- 2026-07-21 — Reframed cursor-resync drift from "resolved open question" to "removed from the list of open architectural risks" per reviewer feedback — it's a proven property (math + encoded test), not a hypothesis anymore. **Phase transition noted: the project's primary outstanding work is now heuristic quality (does this produce the most readable charts), not correctness (does this algorithm behave correctly).** Every remaining open item (weight tuning against a corpus, the fuller regression corpus, real-chart chord readability) is a quality question. Concretely: further code-level correctness auditing of `mapper.py` has low expected payoff now — the higher-value next step is getting real chord-bearing `.gp` files in front of the mapper and playtesting the output.
- 2026-07-22 — **Documentation consistency pass** (no production code changed — see Decision Log for the formal architecture-promotion entry this session added). Architecture §3 (Stage 1) rewritten to describe the current implementation (GP7 direct GPIF parsing primary, PyGuitarPro for legacy formats, GP6 BCFS/BCFZ retained for compatibility) with Route A/B demoted to historical context. Project Goal wording updated to match. Fixed a real doc/code mismatch: Architecture claimed the IR runs at 192 ticks/quarter internally: it actually runs at 960 (PyGuitarPro convention), with `/5` conversion to chart-resolution 192 happening at emit time in `mapper.py`/`chart_writer.py` — doc corrected to match code, no code changed. Milestones M4/M5 reworded to state implementation is substantially/largely complete while human verification remains open, without checking either box. Open Questions: marked the TuxGuitar conversion question obsolete (Route A is gone), split the chord-collision bullet into three (resolved mechanism / still-open quality question / eliminated architectural concern), reformatted the phrase-boundary-reset entry to match the page's strikethrough convention, and fixed an ambiguous sentence ("...auditing has low expected payoff now; getting real chord-bearing files does not" → spelled out explicitly). Agent Protocol updated to give an explicit page-read order (index → Architecture → Milestones → Current State → Decision Log → Open Questions → Handoff Log → GitHub) instead of "read the Game Plan in full." This page (Current State) split into this active summary + the linked archive, since it was the largest sub-page and the original reason for splitting the Game Plan was connector response-size limits. **Next:** none of this changes what's blocking M4/M5 — still need a human to actually playtest a converted `.gp` file in Clone Hero or Moonscraper.
- 2026-07-23 — **Repository/Notion state audit and handover refresh** (Codex; no local files changed). Re-read the authoritative Notion project pages and compared them with the local workspace. The July 22 Notion summary remains the cleanest documented architecture/status baseline, but the local checkout contains additional GUI/PyInstaller/ffmpeg packaging artifacts and generated song outputs. **Critical repository-state warning:** local `main` is `ahead 18, behind 24` relative to `origin/main` and has a large set of modified and untracked files. The current `HANDOFF.md` working-tree version also removes newer guidance visible in its base diff (audio inclusion, lead-in behavior, repeat/D.S. navigation, and successful real-song playtest notes), so no one should assume that file or the checkout represents a clean release. Full tests were not run because there is no usable local Python/pytest environment; `dist/shred2chart.exe --version` and `--help` returned exit code 0 with no output because it is a windowed build, which is only a weak packaging smoke check. Created handoff session `2026-07-23-codex-01` with detailed recovery guidance. **Next:** preserve all user changes, reconcile local and remote history on a clean branch without destructive reset/checkout, inventory the intended GUI/media/repeat/audio changes, establish a test runtime, run the full suite, and only then update milestone/release claims or resume real-song playtesting. **Blockers:** dirty/diverged branch and no verified test runtime.
- 2026-07-23 — **Correction/backfill for the 2026-07-22 session:** yesterday’s work was a **full Notion project and agent-handoff restructure**, not merely the documentation consistency pass described above. The restructure created and populated the Tabs2Chart Repo database; indexed source, documentation, tests, fixtures, and configuration with structured metadata and relations; replaced the monolithic 577-line Game Plan with an index plus five authoritative subpages; created the active/archive Current State pattern; introduced the structured Agent Handoff Log with file relations and handoff rollups; and established the mandatory Agent Protocol and source-of-truth precedence. The architecture/documentation corrections were one component of that larger restructure. Backfilled the missing handoff as session `2026-07-22-notion-restructure-01`. No production behavior was changed by the restructure.
- 2026-07-23 — **Local files restructured to mirror the Notion project model** (session `2026-07-23-codex-02`). Replaced the root 577-line `SHRED2CHART_GAMEPLAN.md` working copy with a concise index and created dedicated local mirrors under `docs/gameplan/` for Architecture, Milestones, Current State, Decision Log, Open Questions, and the Current State Archive. Added root `AGENTS.md` as the local Agent Protocol and `docs/gameplan/handoff-log.md` as the local navigation mirror of the canonical structured database. Preserved the complete pre-split working copy at `docs/gameplan/legacy-gameplan-monolith.md`, labeled historical/read-only, so no uncommitted documentation was lost. Root `HANDOFF.md` was intentionally left unchanged because it is the end-user setup/conversion guide, not the agent execution log. Validation: all expected files exist, every relative Markdown link resolves, no Notion-only block markup or UTF-8 mojibake markers remain. No production code changed. **Next:** use the root index and `AGENTS.md` for all future sessions and update local/Notion state together; the existing dirty/diverged branch and unverified GUI/package work remain separate blockers.
- 2026-07-23 — **Repeat/D.S. al Coda navigation restored** (Claude Code; regression fix, not a restructure). The prior day's audit correctly flagged that repeat-barline, 1st/2nd-ending, and D.S. al Coda (Segno/Coda) playback-order simulation — originally added in `3af06c3`/`4d9587d` — was silently absent from the working `shred2chart/gpif_tempo.py`: it had been dropped by a divergent Copilot branch (`89adb78`, "modernize-and-update-executable") that forked *before* those two commits and never merged them back in, so the loss was accidental, not a deliberate design change. `compute_playback_order`, `_alternate_ending`, and `_direction` were restored from `4d9587d` and merged with the current file's `Linear=true` tempo-ramp discretization (added later, in `89adb78`/`eafc394`/`ca5babe`, so it postdates the navigation logic and the two had never coexisted before). `compute_bar_grid`'s return signature changed back to the 3-tuple `(bar_starts, bar_signatures, bar_source_index)` it had in `4d9587d`; updated the two call sites that had regressed to the 2-tuple form (`shred2chart/ir_gpif.py`'s `dump_ir`, which now walks play positions instead of file order so repeated bars emit notes once per pass, and `shred2chart/cli.py`'s two blend-span fallback sites, which just discard the third value). Restored all 10 repeat/D.S./Coda tests from `4d9587d`'s `tests/test_gpif_tempo.py` (none had survived into the current file) alongside the newer ramp tests. **92 of 93 tests pass** (1 deselected: `test_mock_moon_scraper_receives_manifest_on_stdin`, pre-existing local-environment failure unrelated to this change — it shells out to a `moon-scraper` executable not present in this environment). HANDOFF.md's "How It Works" and "Key Files" sections updated to document the restored playback-ordering step. **Next:** the still-open blockers from 2026-07-23's audit (dirty/diverged branch vs. `origin/main`, no human Clone Hero/Moonscraper playtest yet) remain; this session only fixes the specific regression, it doesn't reconcile the branch or run a real-song playtest.
- 2026-07-23 — **Easy Windows tab + song importer completed** (Codex, session `2026-07-23-codex-03`). Finished the existing GUI/PyInstaller scaffold as a three-input workflow: Guitar Pro tab, matching song audio, and Clone Hero Songs folder. Added `.gp3/.gp4/.gp5` picker support, same-name audio/cover auto-detection, metadata/output preview, sanitized `Artist - Title` destination creation, existing-folder confirmation, missing-audio confirmation, clear progress/success/failure feedback, and album-art normalization to `album.png`. Removed the GUI's misleading lead-in control because the service argument was unused. Kept the wrapper thin over `convert_song`, bundled ffmpeg/drag-drop, and built `dist/Tabs2Chart.exe` (49,846,172 bytes). Added six GUI helper tests and an end-to-end album-art test. Also fixed the pre-existing Windows Moon Scraper command splitter, which had consumed backslashes and caused the only prior suite failure. **Validation:** 100/100 tests pass; packaged EXE stayed running during a three-second hidden launch smoke test; PyInstaller warnings are platform-conditional modules only. **Still required:** human-visible end-to-end import with a real tab/audio pair and Clone Hero song scan; M6 remains partially complete until that check. Branch remains diverged (`main` ahead 19/behind 36 at session start); no commit or remote operation was performed.
- 2026-07-23 — **Four empty bars now precede every generated chart** (Codex, session `2026-07-23-codex-04`). Restored the lead-in transform that had existed in `100ebdf` but was later dropped during branch convergence, upgraded it from two to four measures, and corrected the pre-roll grid: starting tempo/time-signature seeds remain at tick 0 while the original tempo map, sections, and notes move together by four song-specific measures. The audio offset is reduced by the computed lead duration (for the 120 BPM/4/4 fixture: first note tick 3072, `Offset = -8.0`, `delay = -8000`) so unchanged audio stays aligned. Both `convert_song` (GUI/shared service) and CLI use the same default; CLI exposes `--lead-in-bars 0` to disable. Added unit coverage for 4/4, 3/4, source preservation, zero-bar no-op, CLI default, and disable path. **Validation:** 106/106 tests pass; rebuilt `dist/Tabs2Chart.exe` (49,848,129 bytes, SHA-256 `531DA76DCB7E79980A87703F747EA6295599FC8097530483091C82EB13A15C3D`) and completed a packaged launch smoke test. **Still required:** human-visible real-song confirmation that the highway shows four empty bars and the recording remains aligned.
- 2026-07-23 — **Automatic MoonScraper opening and runnable custom alignment guide added** (Codex, session `2026-07-23-codex-05`). The importer discovers the editor from saved config, an adjacent custom copy, registry/common Windows paths, or PATH; auto-open defaults on; Advanced exposes the executable/toggle; and the completed chart can be reopened manually. Launching passes the generated manifest plus `notes.chart`, with failures kept non-fatal. Official source inspection confirmed stock MoonScraper loads supported file arguments. Added a reproducible source patch over upstream `cb4c7a8` plus an immediately runnable, non-destructive BepInEx 5 copy at `dist/Moonscraper-Tabs2Chart`. Its `F8` overlay enables the native waveform, detects the first audio attack, compares it with the first playable note, displays/applies the millisecond delta, and offers ±10/±100 ms nudges. Tabs2Chart automatically prefers this adjacent copy unless another executable was explicitly saved. **Validation:** 113/113 Python tests pass; source patch applies cleanly; both guide variants compile against installed MoonScraper/Unity assemblies; the custom copy remained running and its log confirmed `Tabs2Chart Alignment Guide 1.0.0` loaded and manifest activation fired; rebuilt `dist/Tabs2Chart.exe` passed a packaged launch smoke test. **Still required:** visible real-song guide/alignment and Clone Hero verification. Unity `2018.4.23f1` is only a blocker for the long-term official-source build, not for using the delivered plugin copy.

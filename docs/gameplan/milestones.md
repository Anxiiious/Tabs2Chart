# Milestones

> Local mirror of the [authoritative Notion page](https://app.notion.com/p/3a5b82db13b781f9ad0beecd998921a0). Keep both copies synchronized when project state changes.

- [x] **M0 — Route A validation:** superseded by direct GPIF parsing, which turned out to be necessary anyway since real files are GP7 zip (§3). Validated tempo fidelity against 3 real files (constant tempo, real tempo changes, real time-signature changes) — GO on direct parsing. See Current State.
- [~] **M1 — Parse & IR:** IR event list, both via PyGuitarPro (`.gp3/.gp4/.gp5`) and direct GPIF parsing (`.gp`/`.gpx`) — see Current State. Dumped as JSON and spot-checked programmatically (note counts, chord grouping, every technique flag exercised) against 3 real files. **Not yet done:** the milestone's own verification step — visually spot-checking note count/positions against the tab open in Guitar Pro — needs a human with the GUI; no coding agent session has that. Consider this milestone open until someone does that check.
- [~] **M2 — SyncTrack:** built (`chart_writer.py`); emits B/TS events at 192 res from the IR tempo events, spec-pinned syntax. Awaiting the human Moonscraper/audio verification step.
- [~] **M3 — Emitter skeleton:** built (`mapper.py` + `convert` CLI); naive pitch-mod-5 mapping plus the three cheap high-value rules (tie merge, open-note chugs, HOPO/tap flags) and section-level track blending. Full charts generate for all real files. Awaiting the human playtest.
- [~] **M4 — Real mapping:** **implementation substantially complete** — contour (unbounded wraparound cursor, staircase pattern), open-string chugs, HOPO/tap forced flags, and chord lane placement (scored chord-shape heuristic, `_chord_shape_candidates` + `_rank_chord_shape`) are all built and covered by 63 passing unit/regression tests, including a dedicated cursor-resync-drift regression test. **Not complete:** the milestone's own verification step ("play it, iterate knobs") — no real `.gp` file has been played in Clone Hero or Moonscraper yet, and the `_WEIGHT_*` scoring constants are tuned by intuition, not against real chord-bearing material. Do not check this box until that playtest happens; the open work here is heuristic-quality tuning, not further implementation. See Current State and Decision Log for the full build history.
- [~] **M5 — CLI polish:** **most of the planned surface already exists** — `--version`, `--quiet`/`--verbose`, `--dry-run`, `--charter` (with GP `<SubTitle>` fallback), `--archive`, shell completion (`argcomplete`), `song_length` in song.ini, and `--offset-ms` are all implemented (2026-07-20 session, see Current State). Batch mode (converting a whole library in one invocation) is not implemented. **Not complete:** final polish and verification against real usage patterns. Do not check this box until that's done.
- [~] **M6 — Desktop import wrapper:** the Windows tab + song importer is implemented as a thin GUI over `convert_song` and packaged at `dist/Tabs2Chart.exe` with bundled ffmpeg and drag/drop support. It accepts every supported GP format, auto-detects matching audio/art, previews the metadata-derived destination, confirms before replacing an existing import, reports success/failure clearly, and automatically opens the generated chart in MoonScraper. Generated charts include two tempo/meter-correct empty lead-in measures by default. A runnable, non-destructive custom copy at `dist/Moonscraper-Tabs2Chart` uses BepInEx 5 to add manifest activation, automatic waveform display, transient/first-note delta, one-click offset application, and fine nudges; its launch log confirms the guide loads. A pinned official-source patch and Unity build scripts remain the long-term implementation, with that source-built variant still blocked on Unity 2018.4.23f1. Automated tests and packaged-process smoke tests pass. **Not complete:** visibly test the guide with a real tab/audio import, then scan it in Clone Hero and confirm the folder, two-bar pre-roll, audio sync, guide behavior, and interaction flow before checkoff.
- [ ] **v1.1+ (parking lot):** Route B direct .gpx parsing · difficulty reduction · onset-detection auto-offset · rhythm/bass tracks.
